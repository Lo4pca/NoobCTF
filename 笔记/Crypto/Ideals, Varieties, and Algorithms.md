# [Ideals, Varieties, and Algorithms](https://dokumen.pub/ideals-varieties-and-algorithms-an-introduction-to-computational-algebraic-geometry-and-commutative-algebra-9780387356501-9780387356518-0387356509.html)

上一次学代数几何时没坚持下来，这次找了一本入门难度的教材，希望能坚持到最后

## Geometry, Algebra, and Algorithms

### Polynomials and Affine Space

2. 设 F₂ 为练习 1 中的域

b. 在 F₂[x, y, z] 中找一个非零多项式，它在 $F^3_2$ 的每一点处取值均为零。尝试找一个涉及全部三个变量的多项式

$x^2y+y^2x+x^2z+z^2x$

验证用的sagemath脚本：
```py
F = GF(2)
points = [(x, y, z) for x in F for y in F for z in F]
R.<x, y, z> = PolynomialRing(F, 3)
f = x^2*y+y^2*x+x^2*z+z^2*x
for point in points:
    value = f.subs({x: point[0], y: point[1], z: point[2]})
    if value != 0:
        print(f"在点 {point} 处不为零：f = {value}")
        break
```

c. 在 F₂[x₁, …, xₙ] 中找一个非零多项式，它在 $F^n_2$ 的每一点处取值均为零。你能找到一个涉及全部 x₁, …, xₙ 变量的多项式吗？

已知 $F_2[x]$ 中 $x^2+x$ 在每一点的取值都为0。增加变量时直接乘上新变量即可： $g\in F_2[x_1,...,x_n]=x_nf$ 。其中f在 $F_2[x_1,...,x_{n-1}]$ 消失

直接加也是可以的。 $g\in F_2[x_1,...,x_n]=f+h$ ，其中f在 $F_2[x_1,...,x_{n-1}]$ 消失 ，h在 $F_2[x_n]$ 中消失

任意组合上述操作也可以得到在 $F_2[x_1,...,x_n]$ 中消失的多项式

3. （需抽象代数知识）设 p 为素数。整数模 p 的环是一个具有 p 个元素的域，记作 $F_p$

b. 用拉格朗日定理证明：对所有 $a ∈ F_p$ - {0}，有 $a^{p−1} = 1$

记 $F_p$ -{0}为 $F_p^{*}$ 。由拉格朗日定理，任意a生成的子群的阶一定整除 $|F_p^{*}|$ ，即整除p-1。设子群阶为d，则存在k使得dk=p-1, $a^{p-1}=(a^d)^k=1$

d. 在 $F_p[x]$ 中找一个非零多项式，它在 $F_p$ 的每一点处取值均为零

$x^p-x$

### Affine Varieties

7. 极坐标中最优美的例子之一是四叶玫瑰线(four-leaved rose)。该曲线由极坐标方程 r = sin(2θ) 定义。我们将证明这条曲线是一个仿射簇(affine variety)

a. 利用 r² = x² + y²，x = r cos(θ) 和 y = r sin(θ)，证明四叶玫瑰线包含在仿射簇 V((x² + y²)³ − 4x²y²) 中。提示：利用 sin(2θ) 的恒等式

题目要求证明曲线上任意一个坐标(x,y)满足(x² + y²)³ − 4x²y²=0

sin(2θ)的恒等式为sin(2θ)=2sin(θ)cos(θ),所以r=2sin(θ)cos(θ)。两边同时乘上 $r^2$ 来凑出r cos(θ)和r sin(θ)： $r\cdot r^2=r^2\cdot 2\sin(θ)\cos(θ)=2r\sin(θ)r\cos(θ)=2xy$

r² = x² + y²,所以 $r^3=(x^2+y^2)^{\frac{3}{2}}$ ,得到等式 $(x^2+y^2)^{\frac{3}{2}}=2xy$ 。两边同时平方， $(x^2+y^2)^3=4x^2y^2$ ,即(x² + y²)³ − 4x²y²=0

b. 现在仔细论证 V((x² + y²)³ − 4x²y²) 包含在四叶玫瑰线中。这比看起来要复杂，因为在 r = sin(2θ) 中 r 可以为负

现在要反过来，证明对所有满足(x² + y²)³ − 4x²y²=0的(x,y)，均可找到r和θ使得r = sin(2θ)。至于为什么要“找”，因为虽然给定一组(x,y)，它们与原点的距离r和与x轴的夹角θ就固定了，但sin(2θ)可能为负数，自然不可能等于非负数的距离r；所以需要利用单个点的极坐标表示不唯一来找到一组满足r = sin(2θ)的极坐标

分类讨论。若sin(2θ)大于0，则套用a的方式即可证明确实有r=sin(2θ)；若sin(2θ)小于0，则采用另一组极坐标 $(r_1,\theta_1)=(-r_0,θ_0+π)$ 。已知 $-r_0=\sin(2θ_0)$ ，有 $\sin(2\theta_1)=\sin(2(θ_0+π))=\sin(2\theta_0)=-r_0=r_1$

（ds解答这题时突然发疯，导致我没弄明白这题，只能抄ds的思路，结果就是这样乱七八糟的）

13. 考虑 ℝ² 中的一个机械臂，由长度分别为 3、2 和 1 的三条臂组成。长度为 3 的臂固定在原点，长度为 2 的臂连接在长度为 3 的臂的自由端，长度为 1 的臂连接在长度为 2 的臂的自由端。机械臂的“手”连接在长度为 1 的臂的末端

b. 确定该机械臂的“状态”需要多少个变量？

六个:(x,y,z,w,m,n)。设长度为3的臂为A，长度为2的臂为B，长度为1的臂为C。那么(x,y)代表A和B的连接处坐标，(z,w)为B和C的连接处坐标，(m,n)为C的末端，或者说“手”的坐标

c. 给出可能状态构成的簇的方程

- $x^2+y^2=9$
- $(z-x)^2+(w-y)^2=4$
- $(m-z)^2+(n-w)^2=1$

d. 利用本节讨论的直观维数概念(intuitive notion of dimension)，猜测状态簇的维数应为多少

书中给出的直觉在第22页，大致是“每个方程会使仿射簇的维数比方程所在的维数降一维”。不过这个直觉并不完全准确，书里也给了反例。根据这个直觉来看的话，状态簇的维数可能是三维？

### Parametrizations of Affine Varieties

1. 写出线性方程组所有解的参数形式
- x + 2y − 2z + w = −1(1)
- x + y + z − w = 2(2)

这类题怎么做可参考第29页，用行变换消去变量。然后注意自由参数的个数 = 未知数个数 − 独立方程个数（方程线性无关）

将原方程组的系数矩阵化为行阶梯形：

$$\begin{pmatrix}1 & 2 & -2 & 1 & | & -1 \cr 1 & 1 & 1 & -1 & | & 2\end{pmatrix}
\rightarrow
\begin{pmatrix}1 & 0 & 4 & -3 & | & 5 \cr 0 & 1 & -3 & 2 & | & -3\end{pmatrix}$$

得到：
- x + 4z - 3w = 5
- y - 3z + 2w = -3.

于是令 z = s, w = t，则
- x = 5 + 3t - 4s
- y = -3 - 2t + 3s
- z = s
- w = t

7. 改写第6题的论证，对 n 维仿射空间中的“球面” x₁² + … + xₙ² = 1 进行参数化。提示：将有 n − 1 个参数

设球的顶点为 $P_0=(0,...,1)$ 。固定 $x_n=0$ 并任取其他的坐标，得 $P_1=(x_1',...,0)$ ，然后连接两点。该线段与球的交点为 $(x_1'',...,x_n'')$

参数化得到的线段： $P_0+t(P_1-P_0)=(tx_1',...,1-t)$ ( $P_1-P_0$ 是代表“直线方向”的n维向量，所以这个式子表示“从 $P_0$ 出发，沿着直线方向走t个距离”)

然后算这条直线与球面的交点。将线的参数化形式代入球方程，求解t来找到直线与球面相交时的参数：
- $(tx_1')^2+...+(1-t)^2=1$
- $t^2(x_1'^2+...+x_{n-1}'^2)+1-2t+t^2=1$
- $t^2(x_1'^2+...+x_{n-1}'^2+1)-2t=0$
- $t(t[x_1'^2+...+x_{n-1}'^2+1]-2)=0$
- t=0或 $t=\frac{2}{x_1'^2+...+x_{n-1}'^2+1}$

将这个t代入直线的参数化方程，得： $x_i''=\frac{2x_i'}{x_1'^2+...+x_{n-1}'^2+1},\forall i$ < n， $x_n''=1-\frac{2}{x_1'^2+...+x_{n-1}'^2+1}=\frac{x_1'^2+...+x_{n-1}'^2-1}{x_1'^2+...+x_{n-1}'^2+1}$

8. 考虑由 y² = cx² − x³ 定义的曲线，其中 c 为某常数。我们的目标是参数化这条曲线

a. 证明一条直线与此曲线相交于 0、1、2 或 3 个点。提示：设直线方程为 x = a 或 y = mx + b

对于竖直线x=a,代入曲线方程 $y^2 = c a^2 - a^3$ 可得：
- $ca^2 - a^3 > 0$ ：$y = \pm \sqrt{ca^2 - a^3}$ ，2个交点
- $ca^2 - a^3 = 0$ ：y = 0，1个交点
- $ca^2 - a^3 < 0$ ：无实数解，没有交点

对于非竖直线y=mx+b，代入曲线方程 $(mx + b)^2 = c x^2 - x^3$ 并展开可得 $x^3 + (m^2 - c)x^2 + 2mbx + b^2 = 0$ 。这是一个三次方程，计重数的情况下在复数范围内一定有三个根且一定有一个实根，所以可能有1-3个交点

综合来看，可能的交点数为0-3

b. 证明一条过原点的非垂直直线，当 $m² \not= c$ 时，恰好与曲线交于另一点

已知三次方程的一个根为x=0，代入得 $b^2=0$ 。则方程化简为 $x^3 + (m^2 - c)x^2 = 0$ 。这个方程的判别式恰好为0，一定有重根。进一步分解得到 $x^2(x + (m^2 - c)) = 0$ 。只要 $m² \not= c$ ，方程就有两个根

c. 现在画垂直线 x = 1。给定该直线上一点 (1, t)，画出连接 (1, t) 与原点的直线。该直线与曲线交于点 (x, y)。从几何角度论证这给出了整条曲线的参数化

由于过原点的非垂直直线一定会与曲线上的另一点相交，任意不过原点的垂直直线（不一定要x=1）上一点(1, t)决定了直线的斜率。 $m^2=c$ 时给出的点为原点

d. 证明由 (c) 部分的几何描述可导出参数化:
- x = c − t²，
- y = t(c − t²)

直线的斜率是t，代回`b`部分的 $x^2(x + (m^2 - c)) = 0$ 中，另一个根便为 $c-t^2$ ;由于b为0， $y=t(c-t^2)$

9. 环索线（Strophoid）是一条被多位数学家研究过的曲线，其三角参数化由下式给出：
- x = a sin(t)
- y = a tan(t)(1 + sin(t))

其中 a 为常数

a. 求出描述该环索线的关于 x 和 y 的方程。提示：若不仔细处理，会得到方程 (a² − x²)y² = x²(a + x)²。要理解为何这不完全正确，请考虑 x = −a 的情况

三角参数化方程转关于x和y的方程的关键是利用三角恒等式消去参数。首先：
- $\sin(t)=\frac{x}{a}$
- $\tan(t)=\frac{y}{a(1 + \sin(t))}=\frac{y}{a(1+\frac{x}{a})}$

代入恒等式 $\sin^2(t)=\frac{\tan^2(t)}{1+\tan^2(t)}$ 得 $\frac{x^2}{a^2}=\frac{\frac{y^2}{a^2(1+\frac{x}{a})^2}}{1+\frac{y^2}{a^2(1+\frac{x}{a})^2}}$ ，化简后是 $\frac{x^2}{a^2} = \frac{y^2}{(a + x)^2 + y^2}$

这个定义和提示给出的“错误方程”其实是一样的，不过有理式额外说明了x不能等于-a

b. 求该环索线的代数参数化

有个叫万能代换（Weierstrass substitution）的东西：设 $u=\tan(\frac{t}{2})$ ，则 $\sin(t)=\frac{2u}{1+u^2},\tan(t)=\frac{2u}{1-u^2}$ 。所以：
- $x=\frac{2au}{1+u^2}$
- $y=-\frac{2 a u (u + 1)}{(u - 1) (u^2 + 1)}$

10. 大约公元前 180 年，狄俄克勒斯撰写了《论取火镜》(On Burning-Glasses)一书，他考虑的曲线之一是蔓叶线(cissoid)。他用此曲线解决了倍立方问题 [见下文 (c) 部分]。蔓叶线方程为 y²(a + x) = (a − x)³，其中 a 为常数

a. 求蔓叶线的代数参数化

观察过定点的旋转直线与曲线的交点是常用的代数参数化方法。曲线上明显的一点是(a,0)，则过该点的非竖直线的方程为y=t(x-a)。代入曲线方程： $(t(x-a))^2(a+x)=(a-x)^3$ ,得x=a（二重根）和 $x = \frac{a - a t^2}{t^2 + 1}$ ,则 $y=-\frac{2 a t^3}{t^2 + 1}$

b. 狄俄克勒斯用如下几何构造描述蔓叶线。给定半径为 a 的圆（以原点为圆心），在 a 与 −a 之间取 x，作直线 L 连接 (a, 0) 与圆上的点 $P = (−x, \sqrt{a² − x²})$ 。由此确定 L 上的点 Q = (x, y)。证明蔓叶线即为所有此类点 Q 的轨迹

需要证明点Q的坐标满足蔓叶线的方程。先找L的斜率： $\frac{\sqrt{a² − x²}}{-x-a}$ ，然后用点斜式得出直线方程： $y=\frac{\sqrt{a² − x²}}{-x-a}(X-a)$ 。代入Q的坐标x，得 $y=\frac{\sqrt{a² − x²}}{-x-a}(x-a)$ (所有出现的x都是一样的，看书中给出的实例图就能发现选定的x等于点Q的坐标x)。两边同时平方， $y^2=\frac{(a - x)^3}{a + x}$ ,符合题目给出的蔓叶线方程

c. 倍立方是经典希腊问题，即尝试用尺规作图构造 ³√2。已知仅用尺规这是不可能的。狄俄克勒斯证明，若额外允许使用蔓叶线，则可构造 ³√2。方法如下：作连接 (−a, 0) 与 (0, a/2) 的直线。此直线与蔓叶线交于点 (x, y)。证明 $2 = (\frac{a − x}{y})^3$ ，即可说明如何用尺规和蔓叶线构造 ³√2

直线的斜率是 $\frac{1}{2}$ ，所以方程是 $y=\frac{1}{2}(x+a)$ 。代入曲线方程： $(\frac{1}{2}(x+a))^2(a + x) = (a − x)^3\Rightarrow\frac{1}{4} (a + x)^3 = (a - x)^3$ ;同时 $y^3=\frac{1}{8}(x+a)^3$ ，利用 $(a+x)^3=4(a-x)^3$ 可得 $y^3=\frac{1}{2}(a-x)^3\Rightarrow 2=(\frac{a-x}{y})^3$

尺规作图可以作出长为任意两个线段之间的比例的线段，因此可作出a-x和y之间的比例 $\sqrt[3]{2}$

14. 若子集 C ⊂ ℝ² 满足对任意 P, Q ∈ C，连接 P 与 Q 的线段也包含于 C，则称 C 为凸集(convex sets)

a. 若 $P = (^x_y)$ 和 $Q = (^z_w)$ 属于凸集 C，证明当 0 ≤ t ≤ 1 时，$t(^x_y) + (1 − t)(^z_w) ∈ C$

连接P和Q的直线的斜率是 $\frac{x-z}{y-w}$ ，这也是从Q点到P点的方向向量 $(^{x-z}_{y-w})$ 。 $t(^x_y) + (1 − t)(^z_w)=t(^{x-z}_{y-w})+(^z_w)$ 等于说“从Q点出发，沿P-Q方向走t倍的位移”。在0 ≤ t ≤ 1时，结果一定在P和Q之间的线段上，因此根据凸集的定义，结果也在C中

16. 圆锥曲线是平面上由二次方程 ax² + bxy + cy² + dx + ey + f = 0 定义的曲线。圆锥曲线包括熟知的圆、椭圆、抛物线和双曲线。现考虑由以下参数方程给出的曲线：
- $x=\frac{(1 − t)^2x_1 + 2t(1 − t)wx_2 + t^2x_3}{(1 − t)^2 + 2t(1 − t)w + t^2}$
- $y = \frac{(1 − t)^2 y_1 + 2t(1 − t)wy_2 + t^2 y_3}{(1 − t)^2 + 2t(1 − t)w + t^2}$

其中 0 ≤ t ≤ 1。常数 w, x₁, y₁, x₂, y₂, x₃, y₃ 由设计工程师指定，我们假设 w ≥ 0。在第 3 章中将证明这些方程参数化了一条圆锥曲线。本练习的目标是对 w, x₁, y₁, x₂, y₂, x₃, y₃ 这些量给出几何解释

e. 证明 $(^{x(\frac{1}{2})}_{y(\frac{1}{2})})= \frac{1}{1 + w}(\frac{1}{2}(^{x_1}_{y_1})+\frac{1}{2}(^{x_3}_{y_3}))+\frac{w}{1+w}(^{x_2}_{y_2})$ 。利用此公式证明 $(^{x(\frac{1}{2})}_{y(\frac{1}{2})})$ 位于连接 (x₂, y₂) 与 (x₁, y₁) 和 (x₃, y₃) 之间的中点所成的线段上

代入 $t=\frac{1}{2}$ 可得 $x = \frac{0.5 (2 w x_2 + x_1 + x_3)}{w + 1}=\frac{1}{w+1}(\frac{1}{2}x_1+wx_2+\frac{1}{2}x_3)$ ; $y = \frac{0.5 (2 w y_2 + y_1 + y_3)}{w + 1}=\frac{1}{w+1}(\frac{1}{2}y_1+wy_2+\frac{1}{2}y_3)$ 。组合成向量的形式即可得到题目要求证明的等式

(x₁, y₁) 和 (x₃, y₃) 之间的中点是 $(x_1+\frac{1}{2}(x_3-x_1),y_1+\frac{1}{2}(y_3-y_1))=(\frac{x_1+x_3}{2},\frac{y_1+y_3}{2})$ ,写成向量形式就是 $\frac{1}{2}(^{x_1}_{y_1})+\frac{1}{2}(^{x_3}_{y_3})$ 。注意到 $\frac{1}{1+w}+\frac{w}{1+w}=1$ ，因此根据14(a)的结论， $(^{x(\frac{1}{2})}_{y(\frac{1}{2})})$ 位于中点与(x₂, y₂)之间的线段上

f. 注意 $(^{x(\frac{1}{2})}_{y(\frac{1}{2})})$ 将该线段分成两段，设其长度分别为 a 和 b。证明 $w = \frac{a}{b}$ ，因此 w 精确地告诉我们曲线在何处穿过该线段。提示：使用距离公式

用距离公式计算 $(^{x(\frac{1}{2})}_{y(\frac{1}{2})})$ 与(x₂, y₂)之间的距离和 $(^{x(\frac{1}{2})}_{y(\frac{1}{2})})$ 与中点的距离会得到非常复杂的式子，但是两者相除后得到的确实是w。过程有些冗长而且是ds算的，这里不作记录

不过我怀疑这题本意不是让我们硬算，所以我又找ds写了个加权平均做法。设A，B是两点， $\alpha,\beta\geq 0$ (不全为0)。定义 $C = \frac{\alpha A + \beta B}{\alpha + \beta}$ ,则：
- $C$ 位于线段 $AB$ 上
- $C$ 到 $A$ 的距离 $a = AC$
- $C$ 到 $B$ 的距离 $b = CB$
- 满足 $\frac{a}{b} = \frac{\beta}{\alpha}$

对于这道题， $\alpha=1,\beta=w$ ,所以 $\frac{a}{b}=\frac{w}{1}=w$

### Ideals

10. 利用讨论三次挠线(twisted cubic)时所用的论证，证明 I(V(x − y)) = ⟨x − y⟩。你的论证应对任意无穷域 k 成立

首先显然 $⟨x − y⟩\subseteq I(V)$ ，因为对任意f=(x-y)g,都有f(t,t)=0

其次，题目提到的论证应该在47页。我以为关键是除法（我看到r就觉得它是remainder），其实关键是把多项式拆成“指定结构+零多项式”。假设 $f\in I(V)$ ,则f(t,t)=0。考虑g(t)=f(t,t),则g(t)为零多项式（单变量多项式若有无穷个根，则为零多项式；多变量多项式可以有无穷个根，仅在整个无限域上取值为零时为零多项式）。现在将 $f(x,y)\in K[x,y]$ 改写为f(x,y)-f(x,x)+f(x,x)；已知在I(V)中的多项式均有f(x,x)=0，若能证明(x-y)可整除f(x,y)-f(x,x)便能完成证明

$f(x,y)-f(x,x)=\sum c_{ij}x^iy^j-\sum c_{ij}x^{i+j}=\sum c_{ij}(x^iy^j-x^{i+j})$ 。对任意单项式，都有 $x^iy^j-x^{i+j}=x^i(y^j-x^j)$ ，而 $y^j-x^j$ 可被y-x=-(x-y)整除；因此任意单项式都可被x-y整除，从而任意 $f(x,y)\in K[x,y]$ 也可被x-y整除， $I(V)\subseteq ⟨x − y⟩$

### Polynomials of One Variable

2. 考虑由域 k 中的元素 a₁, …, aₙ 所确定的 n × n 范德蒙德行列式:

$$det\begin{pmatrix}
1&a_1&a_1^2&...&a_1^{n-1} \cr
1&a_2&a_2^2&...&a_2^{n-1} \cr
\vdots&\vdots&\vdots&&\vdots \cr
1&a_n&a_n^2&...&a_n^{n-1}
\end{pmatrix}$$

证明当各 $a_i$ 互不相同时，该行列式非零。提示：若行列式为零，则列向量线性相关。证明该线性关系的系数确定了一个次数 ≤ n−1 的多项式，且该多项式有 n 个根。然后使用`Corollary 3`

若行列式为0，则存在n个 $c_i$ ，使得：

$$c_0 \begin{pmatrix} 1 \cr 1 \cr \vdots \cr 1 \end{pmatrix} + c_1 \begin{pmatrix} a_1 \cr a_2 \cr \vdots \cr a_n \end{pmatrix} + c_2 \begin{pmatrix} a_1^2 \cr a_2^2 \cr \vdots \cr a_n^2 \end{pmatrix} + \cdots + c_{n-1} \begin{pmatrix} a_1^{n-1} \cr a_2^{n-1} \cr \vdots \cr a_n^{n-1} \end{pmatrix} = \begin{pmatrix} 0 \cr 0 \cr \vdots \cr 0 \end{pmatrix}$$

第i行对应的多项式是 $c_0+c_1a_i+...+c_{n-1}a_i^{n-1}=0$ ，对每个i=1..n都成立。但`Corollary 3`说明n-1次多项式最多只能有n-1个根，矛盾。因此行列式不可能是0

3. 考虑理想 I = ⟨x, y⟩ ⊂ k[x, y]。证明 I 不是主理想。提示：若 x = fg，其中 f, g ∈ k[x, y]，证明 f 或 g 必为常数

如果I是主理想，则I = ⟨x, y⟩=⟨g⟩，存在f使得x=fg。因为deg(x)=deg(f)+deg(g)而deg(x)=1，f和g中必有一个常数。同样的论证可用在y上，这说明g只能是常数（若g不是常数，g就必须同时是x的常数倍和y的常数倍；而这不可能，因为x和y不是彼此的常数倍）。然而若g是常数，它生成的理想将是整个k[x, y]，不等于⟨x, y⟩（不包含常数项非零的多项式）。因此I不是主理想

## Groebner Bases

### Introduction

5. 本题将证明 k² 中所有多项式参数曲线均包含于仿射代数簇中

a. 证明 k[x, y] 中不同且次数 ≤ m 的单项式 $x^a y^b$ 的个数等于 (m+1)(m+2)/2。[注：此为二项式系数 $\binom{m+2}{2}$ ]

假设a=0，那么b就有m+1种可能（0到m）；a=1时b有m种可能(0到m-1)……一直到a=m，b就只有一种可能。把所有情况的可能数加起来，就是前m+1个正整数的和。用数列求和公式可得总数为 $\frac{(m+1)(m+2)}{2}$

b. 证明若 f(t) 与 g(t) 是 t 的次数 ≤ n 的多项式，则对足够大的 m，满足 a+b ≤ m 的“单项式“ $[ f(t)]^a [g(t)]^b$ 线性相关

$f(t)^ag(t)^b$ 最大的次数为mn，因此存在于mn+1维的向量空间（基 $(1,t,...,t^{mn})$ ）

利用上一题的结论，所有 $f(t)^ag(t)^b$ 的数量为 $\binom{m+2}{2}$ ，增长速度远超向量空间的维度。于是在m足够大时，当多项式的数量超过向量空间的维度，这些多项式必然线性相关

c. 由 (b) 推断：若 C: x = f(t), y = g(t) 是 k² 中任一多项式参数曲线，则 C 包含于某 F ∈ k[x, y] 的 V(F) 中

定义 $F(x,y)=\sum_{a+b\leq m}c_{a,b}x^ay^b\in k[x,y]$ 。代入x = f(t), y = g(t)，得 $F(x,y)=\sum_{a+b\leq m}c_{a,b}f(t)^ag(t)^b\in k[x,y]$ ，由(b)的结论，可知结果为0。这意味着参数曲线C上的每一点均满足F(x,y)=0，即 $C\subseteq V(F)$

d. 推广本题的 (a)、(b)、(c) 部分，证明任一多项式参数曲面 x = f(t, u), y = g(t, u), z = h(t, u) 均包含于某代数曲面 V(F) 中，其中 F ∈ k[x, y, z]

模仿前三个部分再来一遍相同的逻辑。首先找k[x,y,z]中不同且次数 ≤ m 的单项式 $x^a y^bz^c$ 的个数：将单项式看成 $x^a$ 和 $y^bz^c$ 两个部分，固定a=0..m，则b+c的值为m..0，套用(a)的公式计算 $y^bz^c$ 的数量然后再加起来就能得到 $\sum_{i=0}^m\binom{i+2}{2}=\binom{m+3}{3}$

线性相关和仿射簇包含的证明与(b)、(c)基本一致，只是多项式从两个变成了三个

### A Division Algorithm in $k[x_1,..., x_n]$

11. 本练习将完整刻画由带余除法算法产生的表达式 f = a₁ f₁ + … + aₛ fₛ + r（在所有此类形式的表达式中）。令 $LM(f_i) = x^{α(i)}$ ，并定义

- $Δ₁ = α(1) + ℤⁿ_{≥0}$
- $Δ₂ = (α(2) + ℤⁿ_{≥0}) - Δ₁$
- ⋮
- $Δₛ = (α(s) + ℤⁿ_{≥0}) - (\cup_{i=1}^{s-1} Δ_i)$
- $\bar{\Delta} = ℤⁿ_{≥0} - (\cup_{i=1}^{s} Δ_i)$

（注意 $ℤⁿ_{≥0}$ 是各 $Δ_i$ 和 $\bar{\Delta}$ 的不交并）

a. 证明 $β ∈ Δ_i$ 当且仅当 $x^{α(i)}$ 整除 $x^β$ ，但对所有 j < i， $x^{α(j)}$ 不整除 $x^β$

我有点没看懂题目用的符号，故记录个例子：假设我们在k[x,y]中, $LM(f_i)$ 为 $x^2y$ ，那么 $\alpha(i)=(2,1)$ 。所以 $\alpha(i)$ 和 $ℤⁿ_{≥0}$ 一样，也是分量大于等于0的n元组

假设 $β ∈ Δ_i$ ，很明显 $x^{α(i)}$ 整除 $x^β$ ，因为 $ℤⁿ_{≥0}$ 中的元组没有负数分量，所以任意 $\beta_k$ ( $\beta$ 的第k个分量)都大于等于 $\alpha(i)_k$ 。而对所有 j < i，必然存在一个k使得 $α(j)_k$ 大于 $\beta_k$ ，否则 $Δ_j$ 将包裹住 $\Delta_i$ ，根据各个 $\Delta$ 的定义， $\Delta_i$ 将为空集。由此可知 $x^{α(j)}$ 不整除β

反过来， $x^{α(j)}$ 不整除 $x^β$ 说明存在一个k使得 $α(j)_k$ 大于 $\beta_k$ ,进而大于 $\alpha(i)_k$ , $\Delta_i\not\subseteq\Delta_j$ 。若 $x^{α(i)}$ 整除 $x^β$ ，说明 $x^β$ 是 $x^{α(i)}$ 的延伸， $β ∈ Δ_i$

b. 证明 $γ ∈ \bar{\Delta}$ 当且仅当没有 $x^{α(i)}$ 整除 $x^γ$

任意固定一个 $ℤⁿ_{≥0}$ ，称其为 $\alpha(z)$ ，根据(a)，不会有任何 $x^{α(i)}$ 整除 $\Delta_z$ 中的元素。因为上述结论对任意 $ℤⁿ_{≥0}$ 中的元组成立，故对任意 $γ ∈ \bar{\Delta}$ ，没有 $x^{α(i)}$ 整除 $x^γ$

若没有 $x^{α(i)}$ 整除 $x^γ$ ，则对所有α(i)都存在一个k，使得 $α(i)_k$ 大于 $\gamma_k$ ，因此 $\gamma$ 不在任何 $\Delta_i$ 中。既然 $ℤⁿ_{≥0}$ 是各 $Δ_i$ 和 $\bar{\Delta}$ 的不交并， $\gamma$ 只能在 $\bar{\Delta}$ 中

c. 证明在带余除法算法算出的表达式 f = a₁ f₁ + … + aₛ fₛ + r 中，对每个 i， $a_i$ 中的每个单项式 $x^β$ 满足 $β + α(i) ∈ Δ_i$ ，且 r 中的每个单项式 $x^γ$ 满足 $γ ∈ \bar{\Delta}$

用(a)的结论证明 $β + α(i) ∈ Δ_i$ ： $\beta\in ℤⁿ_{≥0}$ ,因此 $x^{\alpha(i)}$ 整除 $x^{β + α(i)}$ 。对于第二个条件，回顾78页描述的算法，若对某个i， $a_i$ 的增值不为0，则说明对任何j < i的j， $x^{\alpha(j)}$ 不整除 $LM(p)=x^{\alpha(p)}$ 。由于β + α(i)的最大值就是 $\alpha(p)$ ， $x^{\alpha(j)}$ 也无法整除其他可能的 $x^{β + α(i)}$ 。由上可得 $β + α(i) ∈ Δ_i$

用(b)的结论证明 $γ ∈ \bar{\Delta}$ ：算法中对γ的定义就是无法被任何 $x^{α(i)}$ 整除的项，直接得到 $γ ∈ \bar{\Delta}$ 的结论

d. 证明恰存在一个满足 (c) 部分所述性质的表达式 f = a₁ f₁ + … + aₛ fₛ + r

假设存在两种(c)中的表示： $f=a₁ f₁ + … + aₛ fₛ + r=a_1'f_1+...+a_s'f_s+r'$ ，两者相减，得 $(a_1-a_1')f_1+...+(a_s-a_s')f_s+(r-r')=0$ ，即 $(a_1-a_1')f_1+...+(a_s-a_s')f_s=r-r'$ 。设 $b_i=a_i-a_i',R=r-r'$ ，有 $b_1f_1+...+b_sf_s=R$ ， $b_i$ 中的每个单项式 $x^{\beta}$ 也满足 $\beta+\alpha(i)\in\Delta_i$ ；R中单项式 $x^γ$ 满足 $γ ∈ \bar{\Delta}$

假设某个 $b_kf_k$ 非零，其最大的首项单项式为 $x^{\beta+\alpha(k)}\in\Delta_k$ 。由于各个 $\Delta_i$ 互不相交，该式无法被消去，更无法等于左边的R，因此只能所有 $b_i=0$ ,从而r=r'

(燃尽了，做这类题还是很吃力。虽然能稍微“看到”为什么，但是无法清晰表达出来)

### Monomial Ideals and Dickson’s Lemma

4. 设 $I=< x^3y^6,x^5y^4,x^6 >\subset k[x, y]$

a. 用`Theorem 5`证明中给出的方法，求 I 的一个理想基

第一步是找理想J，为I在k[x]上的投影。 $J=< x^3 >\subset k[x]$ 。最大的使得 $x^{\alpha(i)}y^m\in I$ 的m是6；接着找切片 $J_k,0\leq k\leq m-1$ ，为由单项式生成的包含 $y^k$ 的理想：
- $J_0=$ { $x^6$ }
- $J_1=J_2=J_3=$ {0}
- $J_4=J_5=< x^5 >$

于是 $I=< x^3y^6,x^5y^4,x^6 >$ 。由此可见，Dickson引理的算法保证找到一个有限基，但不保证它比原来的基更小或不同

b. 你的基是否已尽可能小？或者能否从中删去某些 β，得到一个更小的仍生成相同理想的集合？

目前的基是极小基，因此没法删去某个元素并保证生成相同理想

11. 本练习将研究权重序的一个特例。设 u = (u₁, …, uₙ) 为 ℝⁿ 中的向量，满足 u₁, …, uₙ 均为正数且在 ℚ 上线性无关。我们称 u 为独立权重向量。对 $α, β ∈ ℤⁿ_{≥0}$ ，定义 $α >_u β ⇔ u·α > u·β$ ，点为通常的向量点乘。我们称 $>_u$ 为由 u 确定的权重序

a. 利用`Corollary 6`证明 $>_u$ 是单项式序（monomial ordering）。提示：你的论证在何处用到了 u₁, …, uₙ 的线性无关性？

86页的`Corollary 6`给出的三条要求中，后两条均不证自明，剩下第一条： > $_u$ 在 $ℤⁿ_{≥0}$ 上是全序（total ordering）。设 $\alpha\neq\beta\in\mathbb{Z}^n_{\ge 0}$ ，则 $u\cdot\alpha$ 和 $u\cdot\beta$ 是实数。若 $u\cdot\alpha = u\cdot\beta$ ，则 $u\cdot(\alpha-\beta)=0$ 。由于 $u_i$ 在 $\mathbb{Q}$ 上线性无关，且 $\alpha-\beta$ 是有理数（整数）向量，只能有 $\alpha-\beta=0$ ，即 $\alpha=\beta$ ，矛盾。因此 $u\cdot\alpha \neq u\cdot\beta$ ，从而要么 $u\cdot\alpha > u\cdot\beta$ 要么 $u\cdot\beta > u\cdot\alpha$ ，全序性成立

（看69页对全序的定义时，我以为一个关系是全序只需要满足“ $x^a$ < $x^b$ , $x^a=x^b$ , $x^a$ > $x^b$ 中有且只有一个说法为真”，但似乎还要保证 $x^a=x^b$ 时，在 $ℤⁿ_{≥0}$ 上a=b）

### The Hilbert Basis Theorem and Groebner Bases

3. 设 I = ⟨f₁, …, fₛ⟩ 是一个理想，满足 ⟨LT(f₁), …, LT(fₛ)⟩ 严格小于 ⟨LT(I)⟩

a. 证明存在某个 f ∈ I，它除以 f₁, …, fₛ 的余式非零。提示：先证存在某个 f ∈ I 使得 LT(f) ∉ ⟨LT(f₁), …, LT(fₛ)⟩，再利用84页的`Lemma 2`

设存在某个f ∈ I，其 $LT(f)\not\in⟨LT(f₁), …, LT(fₛ)⟩$ 。由于⟨LT(f₁), …, LT(fₛ)⟩是单项式理想，这说明LT(f)不被任何LT(f₁), …, LT(fₛ)整除，LT(f)是余式的一部分，故余式非零

12. 证明：若假设 k[x₁, …, xₙ] 中每条理想升链都稳定，则希尔伯特基定理(Hilbert Basis Theorem)的结论可作为推论得出。提示：用反证法，假设某理想 I ⊂ k[x₁, …, xₙ] 没有有限生成集。你的论证不应特别用到多项式的任何性质

如果某个 I ⊂ k[x₁, …, xₙ] 没有有限生成集，则取非零的 $f_1\in I$ ,得 $I_1=<f_1>$ 。由于I不是有限生成的， $I_1\subsetneq I$ 。再取 $f_2\in I/I_1$ ，令 $I_2=<f_1,f_2>$ ……这样继续下去便可得到严格升链 $I_1\subsetneq I_2\subsetneq...$ 。但题目假设每条理想升链都稳定，矛盾。因此I必然是有限生成的

### Properties of Groebner Bases

11. 设 f, g ∈ k[x₁, …, xₙ]， $x^α, x^β$ 为单项式。验证 $S(x^α f, x^β g) = x^γ S(f, g)$ ，其中 $x^γ = \frac{LCM(x^α LM(f), x^β LM(g))}{LCM(LM(f), LM(g))}$ 。务必证明 $x^γ$ 是单项式

S多项式的定义在第97页。" $x^γ$ 是单项式"这点挺明显的，毕竟 $x^α, x^β$ 都是单项式，LM(f),LM(g)也是。假设 $x^{\gamma'}=LCM(x^αLM(f),x^βLM(g))$ ,则 $S(x^α f, x^β g)=\frac{x^{\gamma'}}{LT(f)}f-\frac{x^{\gamma'}}{LT(g)}g$ 。接着让 $x^{\gamma''}=LCM(LM(f),LM(g))$ ，有 $S(f, g)=\frac{x^{\gamma''}}{LT(f)}f-\frac{x^{\gamma''}}{LT(g)}g$ 。注意到 $x^γ = \frac{x^{\gamma'}}{x^{\gamma''}}$ ，因此 $x^γ S(f, g)=S(x^α f, x^β g)$

### Buchberger’s Algorithm

7. 固定一个单项式序，令 G 和 G' 是理想 I 的极小Groebner基

a. 证明 LT(G) = LT(G')

见88页第8题的结论：因为< LT(G) >和< LT(G') >均等于< LT(I) >且单项式理想的极小生成基是唯一的，所以LT(G)=LT(G')

10. 设 $A = (a_{ij})$ 为 n×m 矩阵，元素取自 k，令 $f_i = a_{i1}x_1 + … + a_{im}x_m$ 为由 A 的行确定的 $k[x_1, …, x_m]$ 中的线性多项式，得到理想 $I = ⟨f_1, …, f_n⟩$ 。采用 $x_1$ > … > $x_m$ 的字典序。令 $B = (b_{ij})$ 为由 A 确定的简化行阶梯形矩阵，并令 $g_1, …, g_t$ 为来自 B 的非零行的线性多项式（故 t ≤ n）。我们要证明 $g_1, …, g_t$ 构成 I 的既约 Gröbner 基(reduced Groebner basis)

a. 证明 $I = ⟨g_1, …, g_t⟩$ 。提示：证明对 A 进行一次行操作所得矩阵的行生成相同的理想

行操作包括：
- 交换两行
- 某行乘以非零常数
- 某行加上另一行的倍数

很明显前两样不会改变生成的理想。对于第三点，假设 $R_i'=R_i+cR_j$ 。 $R_i'$ 可以生成 $R_i=R_i'-cR_j$ ，因此整个理想仍然是相同的

b. 用99页的定理 6 证明 $g_1, …, g_t$ 构成 I 的 Gröbner 基。提示：若 B 的第 i 行的首 1 在第 k 列，可写 $g_i = x_k + A$ ，其中 A 是不含对应于各首 1 的变量的线性多项式。若类似地写 $g_j = x_l + B$ ，则需将 $S(g_i, g_j) = x_l A − x_k B$ 除以 $g_1, …, g_t$ 。注意，除法中将只用到 $g_i 和 g_j$

$x_l A − x_k B$ 一定无法被除 $g_i,g_j$ 之外的多项式整除，因为A和B均不含对应于各首1项的变量。若 $g_i=0$ ，则 $x_k=-A$ 。那么 $S(g_i, g_j) = x_l A − x_k B=x_lA+AB=A(x_l+B)$ ，可被 $g_j$ 整除； $g_j$ 同理

（做这道题时我思路完全跑偏了，想的是“对于 $x_lA$ 的每一项 $x_la_i$ , $a_ig_j=a_i(x_l+B)$ 可消去； $x_kB$ 的每一项同理。最后会剩下完全不涉及任何主元变量的多项式”，然后就卡住了。说明这个复杂的余式最终会化简为0太困难了）

c. 解释为什么 $g_1, …, g_t$ 是既约 Gröbner 基

因为 $g_i = x_k + A$ ，首项之间无法整除，A也无法被任何首项整除（106页定义5）

### First Applications of Groebner Bases

7. 设 ℝ³ 中的曲面 S 由连接直线 {x=t, y=0, z=1} 与 {x=0, y=1, z=t} 上具有相同参数 t 的点对所成的直线段之并构成。（这是一类称为直纹面(ruled surfaces)的曲面的特例）

a. 证明曲面 S 可表示为如下参数形式：
- x = ut
- y = 1 − u
- z = u + t − ut

首先得澄清题目到底在说什么。 $L_1=$ {x=t, y=0, z=1} 和 $L_2=$ {x=0, y=1, z=t}分别是两个 ℝ³ 中的直线方程。对每个参数t，取 $P_1(t)=L_1(t)$ 和 $P_2(t)=L_2(t)$ ，连接两点即可得到一条直线段。所有这些直线段的并集就是曲面S

回顾“Parametrizations of Affine Varieties”的第7题，参数化一条直线可以用 $L_t=P_0+u(P_1-P_0)$ ,那么整合起来就是 $S(u,t)=(t,0,1)+u(-t,1,t-1)=(t-ut,u,ut-u+1)$ 。这与期望的参数化不同，因为u的方向反了。 $u\in [0,1]$ ，把u替换为1-u，就能得到题目要求的参数形式

b. 用例 4 和例 5 的方法，求包含曲面 S 的簇 V 的一个（隐式）方程

```py
R.<x,y,z,u,t> = PolynomialRing(QQ, order='lex')
f1 = x-u*t
f2 = y-1+u
f3 = z-u-t+u*t
I = ideal(f1, f2, f3)
print(I.elimination_ideal([u, t]))
```

$xy + y^2 + yz - 2y - z + 1=0$

c. 证明 V = S（即证 V 的每一点均可由 (a) 部分方程代入某些 t, u 的值得到）。提示：尝试将 V 的隐式方程关于一个变量解出，表示为另外两个变量的函数

```py
var('x y z u t')
f = x*y + y^2 + y*z - 2*y - z + 1
sols = solve(f == 0, y)
for sol in sols:
    expr = sol.rhs()
    subs_dict = {
        x: u*t,
        y: 1 - u,
        z: u + t - u*t
    }
    print(expr.subs(subs_dict).simplify_full())
```
x和z的结果均符合预期，只有y的结果不是很直观，为 `-1/2*t - 1/2*u - 1/2*sqrt(t^2 - 2*t*u + u^2) + 1`。放到wolfram alpha里可以看见在t和u均为实数的前提下，原函数等于如下的分段函数：
- 1-t( $t\geq u$ )
- 1-u (其他)

### (Optional) Improvements on Buchberger’s Algorithm

3. 设 M 为 k[x₁, …, xₙ] 上的 m × (m+1) 多项式矩阵。令 I 为由 M 的所有 m×m 子矩阵的行列式生成的理想（这类理想称为行列式理想）

c. 给出生成行列式理想生成元间合冲关系（syzygy）的一般方法。提示：寻找构造包含 M 的 (m+1)×(m+1) 矩阵的方法，其行列式自动为零

“行列式自动为零”指的是行向量线性相关或有两行相同导致行列式恒为0。所以我们可以随便拿M中的一个行向量加到M的底部，得到M'，然后沿新加的那一行做Laplace展开： $det(M')=\sum_{j=1}^{m+1}(-1)^{(m+1)+j}a_{(m+1)j}det(M'_{(m+1)j})$ ， $M'_{(m+1)j}$ 为M'去掉m+1行，j列所成的矩阵，正好就是原M的所有m×m 子矩阵。已知det(M')一定为0，所以 $(-1)^{(m+1)+j}a_{(m+1)j}$ 就是要求的syzygy

7. 完成`Proposition 10`的证明(122页)，证明 $S_{ij}=\frac{x^{\gamma_{ij}}}{x^{\gamma_{ik}}}S_{ik}-\frac{x^{\gamma_{ij}}}{x^{\gamma_{jk}}}S_{jk}$

$S_{ij}$ 表示只有i和j分量不为0，且 $S_{ij}G$ 会消掉G中 $f_i$ 和 $f_j$ 的首项。观察 $S_{ik}$ 和 $S_{jk}$ 的结构，显然需要以某种手段消掉k处的分量。假设 $h_k=(S_{ik})_k,h_k'=(S_{jk})_k$ ,则由于 $LT(h_k)=\frac{x^{\gamma_{ik}}}{LT(f_k)}$ , $LT(h_k)LT(f_k)=x^{\gamma_{ik}}$ ；类似地，有 $LT(h_k')LT(f_k)=x^{\gamma_{jk}}$ 。不难看出分别乘上 $\frac{x^{\gamma_{ij}}}{x^{\gamma_{ik}}}$ 和 $\frac{x^{\gamma_{ij}}}{x^{\gamma_{jk}}}$ 后，两者即可抵消

接下来需要证明 $S_{ij}$ 确实可以取消掉 $f_i,f_j$ 的首项。这不难，因为任何syzygy乘以任意单项式后，仍然是针对对应首项的syzygy。所以 $(S_{ik})_i$ 和 $(S_{jk})_j$ 分别乘上 $f_i$ 和 $f_j$ 后结果的首项仍等于其k分量乘上 $f_k$ 的结果的首项，而后者已被证明相等

## Elimination Theory

### The Geometry of Elimination

1. 用Extension Theorem(132)和Lemma 1(137)证明Geometric Extension Theorem(138)

脑抽了，想了半天 $V(I_1)$ 是如何拆成 $\pi_1(V)$ 和 $V(g_1,..., g_s) ∩ V(I_1)$ 的，不断地钻 $V(g_1,..., g_s) ∩ V(I_1)$ 的牛角尖：“啊，为什么是这两者的交集呢”，事实上这题根本没有什么复杂的内容

给定任意 $a=(a_2,...,a_n)\in V(I_1)$ ,若 $a\not\in V(g_1,...,g_s)$ ,则根据Extension Theorem，a可扩展。可扩展说明存在 $a_1$ 使得 $(a_1,a_2,...,a_n)\in V$ ，即 $a\in\pi_1(V)$

若 $a\in V(g_1,...,g_s)$ ，我们无法得知a是否可扩展，它可能不在 $\pi_1(V)$ 中。那么到底有什么a在 $V(I_1)$ 中呢？答案是两者的交集，这就是 $V(g_1,..., g_s) ∩ V(I_1)$ 的由来……

### Implicitization

4. 正文中已证明，在复数域 ℂ 上，三次挠线的切线面由方程 g₇ = x³z − (3/4)x²y² − (3/2)xyz + y³ + (1/4)z² = 0 定义。我们想证明在 ℝ 上结论同样成立。若 (x, y, z) 是上述方程的实数解，则由扩张定理可证存在 t, u ∈ ℂ 使得：
- x = t + u
- y = t² + 2tu
- z = t³ + 3t²u

利用文中给出的 Gröbner 基（145）证明 t 和 u 是实数。这将证明 (x, y, z) 在 ℝ³ 中的切线面上。提示：先证明 u 是实数

$g_3=ux^2 − uy − x^3 + (3/2)xy − (1/2)z$ ，隔离出u就能发现，由于x，y和z均为实数，u自然也是实数

现在已知x和u都是实数，利用x=t+u就能直接得出t也是实数

（我忘了考虑 $x^2-y=0$ 的情况，让ds补上：）

若 $x^2-y=0$ ，即 $y=x^2$ ，代入切线面方程 $g_7=x^3z-\frac34x^2y^2-\frac32xyz+y^3+\frac14z^2$ 可得 $g_7=\frac14(z-x^3)^2=0$ ,所以 $z=x^3$ 。此时可取u=0,t=x,满足参数化且均为实数

（我第一反应是用 $g_2$ ，因为它看起来最简洁；但我忽略了 $g_2$ 的u是平方，证明 $x^2\geq y$ 就不是很容易了）

9. Whitney伞面(umbrella surface)由如下参数方程给出：
- x = uv
- y = v
- z = u²

a. 求包含 Whitney 伞面的最小簇的方程

$x^2 - y^2z=0$

b. 证明该参数化在 ℂ 上填满该簇，但在 ℝ 上并非如此。在 ℝ 上，究竟遗漏了哪些点？

参考145页对twisted cubic的切面的论证，需利用Extension Theorem一个一个确认能否扩展至v和u。首先找Groebner Bases：

```py
R.<u,v,x,y,z> = PolynomialRing(QQ, order='lex')
f1 = x-u*v
f2 = y-v
f3 = z-u^2
I = ideal(f1, f2, f3)
print(I.groebner_basis())
#[u^2 - z, u*x - y*z, u*y - x, v - y, x^2 - y^2*z]
```
v-y和 $u^2 - z$ 两个多项式能够说明可从(x,y,z)扩张至(u,v,x,y,z)

在 $u^2 - z=0$ 中，若z小于0，则u没有实数解。而簇方程存在z小于0的解：(0,0,z)，因此参数化在R上并没有填满该簇，遗漏了负z轴的部分

c. 证明参数 u 和 v 并非总能由 x、y、z 唯一确定。找出唯一性失效的点，并说明你的答案与图形(150)的关系

从 $u^2 - z$ 可看出u有两个可能值: $\pm\sqrt{z}$ （除非z=0）。剩下两个约束u的多项式分别为ux - yz和uy - x。当x和y均为0且z大于0时（上一题已经说明了簇内存在这样的解），u仍然有两个值，因此u不是唯一确定的

（但v似乎是由y唯一确定的？）

上述现象对应150页的图像中面与面重叠的部分（所以图像的竖线为z轴？）。由于两条不同v参数线 $u=\pm\sqrt{z}$ 在此处相交，曲面在此处折叠

我到现在才明白原来图像上的网格是参数线（我还以为画出来是因为好看……）。u参数线固定v，让u变化；v参数线则是反过来

10. 考虑 ℂⁿ 中由 $x_i = f_i(t)$ 参数化的曲线，其中 $f_1, …, f_n$ 为 ℂ[t] 中的多项式。由此得到理想 I = $⟨x_1 − f_1(t), …, x_n − f_n(t)⟩ ⊂ ℂ[t, x_1, …, x_n]$

a. 证明这组参数方程填满整个簇V(I₁) ⊂ ℂⁿ

参考上一题，仍然要用Extension Theorem确认是否能完全扩张。虽然多项式的具体结构未知，但如果把 $x_i-f_i(t)$ 看作关于t的多项式（系数在 $C[x_i]$ 中）： $x_i-f_i(t)=-c_dt^d+...+(x_i-c_0)(c_d\not=0)$ ，就能发现首项 $t^d$ 的系数是常数项，进而利用Corollary 4（134）推出参数方程组填满整个簇

b. 证明若允许 $f_1, …, f_n$ 为有理函数，(a) 部分的结论可能不成立。提示：参见第 1 章 §3

提示指的是32页的单位圆参数化：
- $x=\frac{1-t^2}{1+t^2}$
- $y=\frac{2t}{1+t^2}$

当t趋近于无穷时，两者的极限分别是-1和0，意味着不存在任何有限t使得x=0，y=1。但簇 $x^2+y^2-1$ 中包含这个点，两者不相等

11. 本题与定理2(148)的证明有关

a. 设 k 为无穷域，f, g ∈ k[t₁, …, tₘ]。假设 g ≠ 0，且 f 在 kᵐ - V(g) 上取值为零。证明 f 是零多项式。提示：考虑乘积 fg

由于f在任何g取值不为零的点上取值为0，所以fg在kᵐ上的所有点取值为0。已知g不是零多项式，那么只能是f为零多项式

b. 利用文中给出的提示证明定理2（144）

其实我并没有完全看懂原证明,任何证明只要长得类似的符号一多我准会迷茫起来。只能跟着提示连猜带蒙，尝试对原证明做个替换

第一段：设 $V=V(J)\subset k^{n+m+1}$ 。假设k=C，由(8)(147)，有 $F(C^m-W)=\pi_{m+1}(V)$ 。根据Closure Theorem（139）， $V(J_{m+1})$ 是 $k^n$ 中包含 $\pi_{m+1}(V)$ 的最小簇

第三段：已知 $F(k^m-W)=\pi_{m+1}(V_k)\subset V_k(J_{m+1})$ 。设 $Z_k=V_k(g_1,...,g_s)\subset k^n$ 是 $k^n$ 中满足 $F(k^m-W)\subset Z_k$ 的任意簇，需证明 $V_k(J_{m+1})\subset Z_k$ 。首先各个 $g_i$ 在 $Z_k$ 上取值为0，所以 $g_i$ 在更小的集合 $F(k^m-W)$ 上取值也为0。这说明每个 $g_i\circ F$ 在 $k^m-W$ 上取值为0。这里就能用(a)的结论得到 $g_i\circ F$ 是零多项式，进而完成全部的证明了

### Singular Points and Envelopes

2. 在定义1(153)中，我们需要证明重数的概念与直线的参数化方式无关

a. 证明两个参数化
- x = a + ct，  x = a + c′t
- y = b + dt，  y = b + d′t

对应同一直线，当且仅当存在非零数 λ ∈ k，使得 (c, d) = λ(c′, d′)。提示：在直线 L 的参数化 x = a + ct, y = b + dt 中，L 与向量 (c, d) 平行

L与向量(c,d)平行的原因是L=(a,b)+t(c,d)：从点(a,b)出发，沿着向量(c,d)的方向移动

$\Rightarrow$ 方向显然。对于 $\Leftarrow$ 方向，若(c, d) = λ(c′, d′)，则两个向量代表同一方向，只是模长不同。既然两者都过点(a,b),两者对应同一条直线

b. 设 (a) 部分的两个参数化对应同一直线 L，且 L 与 V(f) 交于 (a, b)。证明多项式 g(t) = f(a + ct, b + dt) 与 g′(t) = f(a + c′t, b + d′t) 在 t = 0 处有相同的重数。提示：利用 (a) 部分建立 g 与 g′ 的关系。这将证明 L 与 V(f) 在 (a, b) 处相交的重数是良定义的

由(a)，得 $g'(t)=f(a + c't, b + d't)=f(a+\frac{c}{\lambda}t,b+\frac{d}{\lambda}t)=g(\frac{t}{\lambda})$

假设t=0是g的具有重数k的根，则 $g=t^kh(t),h(0)\not=0$ 。那么 $g'(t)=g(\frac{t}{\lambda})=(\frac{t}{\lambda})^kh(\frac{t}{\lambda})=\frac{1}{\lambda^k}t^kh(\frac{t}{\lambda})$ 。其中 $\frac{t}{\lambda}=0$ 而 $h(0)\not=0$ ，因此g'在t=0处的重数也是k

3. 考虑直线
- x = t
- y = b + t

其斜率为 1，纵截距为 b。当 b 取何值时，该直线与圆 x² + y² = 2 相切？提示：考虑 g(t) = t² + (b + t)² − 2。该二次式的根确定直线与圆相交时 t 的值

153页有这样一句话：“Thus, we can detect when the line is tangent to the parabola by looking for a multiple root”。所以我们希望找到一个b值，使得g(t)有重根。有重根对应判别式为0。g(t)的判别式为 $-4(b^2-4)$ ,解得 $b=\pm 2$

4. 若 (a, b) ∈ V(f) 且 $∇f(a, b) \not= (0, 0)$ ，证明 V(f) 在 (a, b) 处的切线由方程 $\frac{\partial}{\partial x} f(a, b) · (x − a) + \frac{\partial}{\partial y} f(a, b) · (y − b) = 0$ 定义

“L=(a+ct,b+dt)是点(a,b)的切线”等同于说“t=0是g(t) = f (a + ct, b + dt)重数大于等于2的根”。而t = 0 is a root of g of multiplicity ≥ 2 ⇔ g'(0) = 0。借用154页出现的方程， $\frac{\partial}{\partial x}f (a, b) · c +\frac{\partial}{\partial y} f(a, b) · d=0$ 。注意到（或者155页给出）∇ f (a, b) · (c, d)=0，说明∇ f (a, b)与向量(c, d)垂直。因为(c,d)与L平行，所以可以用点法式方程得出过点(a,b)，法向量为∇ f (a, b)的直线为 $\frac{\partial}{\partial x} f(a, b) · (x − a) + \frac{\partial}{\partial y} f(a, b) · (y − b) = 0$

7. 本练习将研究切线与曲线的相交重数如何大于 2。设 C 为由 y = f(x) 定义的曲线，其中 f ∈ k[x]。因此 C 即 f 的图像

a. 用代数方法证明 C 在 (a, f(a)) 处的切线可参数化为
- x = a + t
- y = f(a) + f′(a)t

提示：考虑 g(t) = f(a) + f′(a)t − f(a + t)

这里说的“代数方法证明”似乎指的是完全通过重数的定义与多项式的代数运算（求导、分解）证明，不涉及极限

F(x,y)=y-f(x),g(t)=F(a+t,f(a) + f′(a)t)=f(a) + f′(a)t-f(a+t)。用求导判定g(t)是否有重根：
- g(0)=f(a)-f(a)=0
- $g'(t)=f'(a)-f'(a+t)$ , $g'(0)=0$

所以g(t)在t=0处的重数至少为2，直线与曲线相切

14. 本题研究例 7 中考虑的曲线族 F = (x − t)² − y + t 的包络(envelope)

a. 显然包络是一条斜率为 1 的直线。用初等微积分证明该直线为 y = x − 1/4

$\frac{\partial}{\partial t}((x − t)² − y + t)=2t-2x+1$ 。定义要求该式等于0，所以 $2t-2x+1=0\Rightarrow t=x-\frac{1}{2}$ 。代回F(x,y,t)中： $(x-(x-\frac{1}{2}))^2-y+(x-\frac{1}{2})=0\Rightarrow y = x - 1/4$

c. 求包络的一个参数化，使得在时刻 t，点 (f(t), g(t)) 位于抛物线 $V(F_t)$ 上。注意，这正是我们讨论定义5(157)时所用的参数化方式

从2t-2x+1得 $x=t+\frac{1}{2}$ ，然后代回原方程得 $y=t+\frac{1}{4}$

19. 本练习将说明我们关于包络的定义过于朴素

c. 当曲线族为 F = (x − t³)² + y² − 1 时，用定义 5 求其包络。注意，该曲线族中的某条曲线是答案的一部分。这是因为使用 t³ 使得曲线在 t = 0 附近“聚集”，这迫使 V(F₀) 出现在包络中

在我们对包络的直观讨论中，曾假设可以对包络进行参数化，使得在时刻 t，点 (f(t), g(t)) 位于 $V(F_t)$ 上。这预设了包络与曲线族中不同的曲线相切。然而在 (c) 部分给出的例子中，包络的一部分位于曲线族中的同一条曲线上。因此，我们对包络的处理过于简单了

```py
R.<x,y,t> = PolynomialRing(QQ, order='lex')
f=(x - t^3)^2 + y^2 -1
df_dt = f.derivative(t)
I = ideal(f,df_dt)
print(I.elimination_ideal([t]))
```
我跑了几个书中的案例，发现sagemath的`groebner_basis`函数的输出和书里不一样。可能是单项式序不一样、域不一样或者约化的程度不一样？

脚本给出的包络其实是曲线族中圆心为原点的那条曲线。在desmos中绘制可以看得更清楚：用一个格子定义`t=[-20,-19.9,...,20]`，然后另一个格子输入题目给出的多项式。由于 $t^3$ 的变化率（导数）在t=0处趋近于0，有大量的圆聚集在圆心周围

我不完全理解这种现象，跟ds聊了一会还是有些云里雾里的，甚至连问题都问不出来。总之，这似乎是因为df_dt在t=0时恒为0，类似155页定义3中奇点的情况

### Unique Factorization and Resultants

6. 在第 1 章 §5 的练习14(61)中，我们证明了若 $f = c(x − a₁)^{r₁} ⋯ (x − a_l)^{r_l} ∈ ℂ[x]$ ，则 $GCD(f, f') = (x − a₁)^{r₁−1} ⋯ (x − a_l)^{r_l−1}$ 。
在任意域 k 上，给定正次数多项式 f ∈ k[x]，未必能分解为线性因式的乘积。但由唯一分解定理，f 可写为 $f = c f₁^{r₁} ⋯ f_l^{r_l}，c ∈ k$ ，其中 $f₁, …, f_l ∈ k[x]$ 均不可约，且当 j ≠ i 时， $f_i$ 不是 $f_j$ 的常数倍。证明：若 k 包含有理数域 ℚ，则 $GCD(f, f') = f₁^{r₁−1} ⋯ f_l^{r_l−1}$ 。提示：仿照第 1 章 §5 练习 14 的思路。证明中需使用唯一分解。假设 ℚ ⊂ k 是为了保证 f′ ≠ 0

假设 $f=g^rh$ ,gcd(g,h)=1。利用链式法则， $(g^r)'=rg^{r-1}g'$ (设u=g)。再用乘积法则， $(g^rh)'=rg^{r-1}g'h+g^rh'=g^{r-1}(rg'h+gh')$ ，f'可写成 $f'=g^{r-1}H$ 的形式，并且gcd(g,H)仍然为1，因为gcd(g,r)=gcd(g,h)=1，加上g不可约，gcd(g,g')同样为1

接下来假设 $f = c f₁^{r₁} ⋯ f_l^{r_l}，c ∈ k$ （唯一分解）。我们可以将其拆分成 $f=f_i^{r_i}h_i$ 的格式。利用刚才的结果，可知 $f_i^{r_i-1}|f'$ ，并且在 $i\not=j$ 时， $f_i|h_j$ 。由此我们可以推断出，对于某个拆分形成的导数 $f'=f_k^{r_k-1}H_k$ ， $f_i|H_k,i\not=k$ ；并且 $gcd(f_i,H_k)=f_i$ 。所以 $f'=f_1^{r_1-1}...f_l^{r_l-1}H$ ，其中 $gcd(f_i,H)=1$

综上所述， $gcd(f,f')=f₁^{r₁−1} ⋯ f_l^{r_l−1}$

根据ds，假设 ℚ ⊂ k 似乎是为了保证域k的特征为0。否则对于特征p的域k， $x^p$ 的导数 $px^{p-1}=0$

15. 证明：若 f 的次数为 l，g 的次数为 m，则结式具有如下对称性： $Res(f, g, x) = (−1)^{lm} Res(g, f, x)$ 。提示：交换两列会改变行列式的符号

交换f和g后得到的Sylvester矩阵的前l列与后m列整体做了交换。我本来想着算算这需要交换两列多少次的，结果发现有多种情况

问了ds，关键原来是一个我很久以前学的玩意：逆序数。每个相邻列交换改变一次符号，而任意置换可分解为若干相邻交换。逆序数就是该置换分解为相邻交换的最少次数的奇偶性。所以不需要管具体是多少列，只需要知道原本在后面的l列跑到了m列前面，每个 $(a_i,b_i)$ 形成一个逆序对，总共是lm对。由此得列置换的符号为 $(-1)^{lm}$

16. 设 $f = a₀x^l + … + a_l$ 和 $g = b₀x^m + … + b_m$ 为 k[x] 中的多项式，且设 l ≥ m

a. 令 $f̃ = f − (a₀/b₀)x^{l−m}g$ ，使得 deg(f̃) ≤ l − 1。若 deg(f̃) = l − 1，证明 $Res(f, g, x) = (−1)^m b₀ Res(f̃, g, x)$ 。提示：对Sylvester矩阵使用列变换。从 f 的各列中减去 a₀/b₀ 乘以 g 的前 m 列。然后沿第一行按子式展开

此处见169页的图（不过g部分的列数似乎标错了，应该是“l columns”）。 $f − (a₀/b₀)x^{l−m}g$ 等于把g的m列移到与f对齐的地方然后f减去a₀/b₀倍g对应的列（所以f的后l-m列没有变化，不过不重要）。由于构造的乘数是a₀/b₀，f的最高项（第l项）一定会被消去，等于图中所有 $a_0=0$ 。这个操作不会改变行列式的值

接下来看g部分的变化。由于deg(f̃) = l − 1，式子(5)中的 $d_0=0$ ，于是我们移除g部分的第一列，得到的就是Syl(f̃, g, x)

沿第一行按子式展开nxn矩阵的通用公式为 $\det(A) = \sum_{j=1}^n (-1)^{1+j} a_{1j} M_{1j}$ ,其中 $M_{ij}$ 为去掉A第i行第j列得到的子式。对于这题，注意到Syl(f̃, g, x)由原Syl(f, g, x)移除第一行第m+1列得到（忽略列相减改变的系数，因为不影响行列式的值），且执行上文描述的列变换后第一行仅第m+1个分量不为0，为 $b_0$ ，因此 $Res(f, g, x) = \sum_{j=1}^{l+m-1} (-1)^{1+j} a_{1j} Syl(f̃, g, x),a_{1j}=0,\forall j\not=m+1\Rightarrow Res(f, g, x) = (-1)^{m+2}b_0Res(f̃, g, x)$ 。 $m+2\equiv m\mod 2$ ，因此可进一步简化为 $Res(f, g, x) = (−1)^m b₀ Res(f̃, g, x)$

b. 令 f̃ 如 (a) 部分所述，但这次允许 f̃ 的次数严格小于l − 1。证明 $Res(f, g, x) = (−1)^{m(l − deg(f̃))} b₀^{l − deg(f̃)} Res(f̃, g, x)$ 。提示：指数 l − deg(f̃) 表示需要按子式展开的次数

假如l − deg(f̃)=k，这相当于图中所有小于k的 $a_i=0$ ,g部分的前k列均被移除。先前的推导中Res(f̃, g, x) 直接等于子式，在k不为1时则须进一步展开。举个例子，假设deg(f̃)=l-2，推导可得到 $Res(f, g, x) = (-1)^{m}b_0M_{1(m+1)}$ ，而 $M_{1(m+1)}$ 按第一行展开也等于 $\sum_{j=1}^n (-1)^{1+j} a_{1j} M'_{1j}$ 。展开两次后， $M'_{1j}=Res(f̃, g, x)$ ，因此代回原式得deg(f̃)=l-2时 $Res(f, g, x) = (-1)^{m}b_0((-1)^{m}b_0Res(f̃, g, x))=(-1)^{2m}b_0^2Res(f̃, g, x)$

一般情况下，展开步骤将持续到 $M^k_{1j}=Res(f̃, g, x)$ ，即展开l − deg(f̃)次。每次展开会给-1的指数贡献m次，给 $b_0$ 的指数贡献1次，因此 $Res(f, g, x) = (−1)^{m(l − deg(f̃))} b₀^{l − deg(f̃)} Res(f̃, g, x)$

## The Algebra–Geometry Dictionary

### Hilbert’s Nullstellensatz

3. 在坐标变换 (1) (185)下，全次数为 N 的多项式 f(x₁, …, xₙ) 变为如下形式的多项式： $f̃ = c(a₂, …, aₙ) x̃₁^N$ + x̃₁ 次数 < N 的各项。我们要证明 c(a₂, …, aₙ) 是 a₂, …, aₙ 的非零多项式

a. 将 f 写为 $f = h_N + h_{N−1} + … + h₀$ ，其中每个 $h_i（0 ≤ i ≤ N）$ 是 i 次齐次多项式（即 $h_i$ 中每个单项式的全次数为 i）。证明经过坐标变换 (1) 后，f̃ 中 $x̃₁^N$ 的系数 c(a₂, …, aₙ) 等于 $h_N(1, a₂, …, aₙ)$

f̃的最高项 $x̃₁^N$ 的系数来自原多项式f中全次数为N的单项式：假设原本的 $x_1^{e_1}...x_n^{e_n}$ 的全次数为N，则 $e_1+...+e_n=N$ ，代换后得 $x̃₁^{e_1}(x̃_2+a_2x̃_1)^{e_2}...(x̃_n + a_n x̃_1)^{e_n}$ 。展开后的式子很复杂，但一定存在 $x̃₁^{e_1+...+e_n}$ 项。所有这样的单项式的涉及 $x̃₁^N$ 的部分之和即为文中提到的 $c(a_2,...,a_n)$

现在具体看 $x̃₁^{e_1}(x̃_2+a_2x̃_1)^{e_2}...(x̃_n + a_n x̃_1)^{e_n}$ 中那些部分会是 $x̃₁^N$ 的系数。要得到 $x̃₁^N$ ,必须在每个 $(x̃_i+a_ix̃_1)^{e_i}$ 项展开的结果中选 $(a_ix̃_1)^{e_i}$ ,于是结果就是 $x̃₁^{e_1}(a_2x̃_1)^{e_2}...(a_nx̃_1)^{e_n}=a_2^{e_2}...a_n^{e_n}x̃_1^N$ ， $c(a₂, …, aₙ)=\sum c_{e_1,...,e_n}a_2^{e_2}...a_n^{e_n}$ ( $c_{e_1,...,e_n}$ 为 $x_1^{e_1}...x_n^{e_n}$ 项的系数)

接下来我们展开 $h_N: \sum_{e_1+...e_n=N}c_{e_1,...,e_n}x_1^{e_1}+...+x_n^{e_n}$ ，不难看出 $c(a₂, …, aₙ)=h_N(1,a₂, …, aₙ)$

b. 设 h(x₁, …, xₙ) 为齐次多项式。证明 h 是 k[x₁, …, xₙ] 中的零多项式，当且仅当 h(1, x₂, …, xₙ) 是 k[x₂, …, xₙ] 中的零多项式

$\Rightarrow$ 显然。对于 $\Leftarrow$ ，假设deg(h)=d,将h写成 $\sum_{e_1+...+e_n=d}c_{e_1,...,e_n}x_1^{e_1}+...+x_n^{e_n}$ 。其实具体代入的 $x_1$ 不重要，只要是非零常数即可， $h(c,x_2,...,x_n)$ 为零多项式 $\Leftrightarrow$ 所有 $c_{e_1,...,e_n}=0$ 。由于齐次多项式中所有单项式有相同的全次数 $d$ ，代入 $x_1 = c$ 后，每个单项式都变成 $c^{e_1} x_2^{e_2} \cdots x_n^{e_n}$ ， $e_1$ 由其他指数唯一确定，不同单项式不会合并（它们的 $x_2, \dots, x_n$ 指数部分不同）。看个反例会更清晰：

$$h(x_1, x_2) = x_1^2 - x_1$$

代入 $x_1 = 1$：
$$h(1, x_2) = 1^2 - 1 = 0$$

$h(1, x_2) = 0$ 是零多项式，但 $h(x_1, x_2) = x_1^2 - x_1 \neq 0$

（做整道大题时脑子不知道去哪了，ds给我讲了好多遍才稍微明白点）

4. 证明代数闭域 k 必为无限域。提示：给定域 k 中的 n 个元素 a₁, …, aₙ，能否写出一个非常数多项式 f ∈ k[x]，使得对所有 i 都有 $f(a_i) = 1$ ？

提示说的 $f=\prod(x-a_i)+1$ 。对于任意多个有限元素，我们都可以这样构造多项式，使得该多项式的根不在这任意多个有限元素里。而k是代数闭域告诉我们必然存在一个根，所以k中存在不在上述有限元素里的元素。这样无限重复下去说明任意有限集合都不是k本身，k必然是无限域

7. 本练习的目的是证明：若 k 是任一非代数闭域，则任何簇 V ⊂ kⁿ 都可由单个方程定义

a. 若 f = a₀xⁿ + a₁xⁿ⁻¹ + … + aₙ₋₁x + aₙ 是 x 中的 n 次多项式，定义 f 关于变量 y 的齐次化(homogenization) $f^h$ 为齐次多项式 $f^h = a₀xⁿ + a₁xⁿ⁻¹y + … + aₙ₋₁xyⁿ⁻¹ + aₙyⁿ$ 。证明：f 在 k 中有根，当且仅当存在 (a, b) ∈ k²，使得 (a, b) ≠ (0, 0) 且 $f^h(a, b) = 0$ 。提示：证明当 b ≠ 0 时， $f^h(a, b) = bⁿ f^h(a/b, 1)$

$f^h(a,b)=a_0a^n+a_1a^{n-1}b+...+a_{n-1}ab^{n-1}+a_nb^n$ 。整体提出 $b^n$ ，得 $a_0(a/b)^n+a_1(a/b)^{n-1}+...+a_{n-1}a/b+a_n=b^nf^h(a/b,1)$

如果f在k中有根r，则对任意满足a/b=r的(a,b)（b不为0）都有 $f^h(a,b)=0$ 。反过来，若存在(a,b) ≠ (0, 0)使得 $f^h(a,b)=0$ ，则r=a/b是f在k中的根

b. 若 k 不是代数闭域，证明存在 f ∈ k[x, y]，使得由 f = 0 定义的簇仅由原点 (0, 0) ∈ k² 组成。提示：选取一个 k[x] 中在 k 中无根的多项式，并考虑其齐次化

继续用a部分构造的f和 $f^h$ 。f在k[x]中的根也是 $f^h(x,1)\in k[x]$ 的根；而 $f^h(x,1)$ 有根同样说明 $f^h(x,y)$ 有y不为0的根。所以如果f在k[x]中无根，则 $f^h$ 唯一的根是(0,0)

c. 若 k 不是代数闭域，证明对每个整数 s > 0，存在 f ∈ k[x₁, …, xₛ]，使得 f = 0 的唯一解是原点 (0, …, 0) ∈ kˢ。提示：使用归纳法及上述 (b) 部分

假设结论对s-1成立，存在多项式 $f_{s-1} ∈ k[x₁, …, x_{s-1}]$ ，其唯一解为原点 $(0, …, 0) ∈ k^{s-1}$ 。现在构造 $f_s=f_2(f_{s-1}(x_1,...,x_{s-1}),x_s),f_s=0\Leftrightarrow f_{s-1}=0,x_s=0$ 。所以其唯一解仍然是原点(0, …, 0) ∈ kˢ

b部分已给出s=2时的多项式

d. 若 W = V(g₁, …, gₛ) 是 kⁿ 中的任一簇，其中 k 非代数闭，证明 W 可由单个方程定义。提示：考虑多项式 f(g₁, …, gₛ)，其中 f 如上所述

令f是 $k[x_1,..,x_s]$ 中满足唯一解为原点的多项式。则 $f(g₁, …, gₛ)=0\Leftrightarrow g_1=...=g_s=0$ ，V(f)=W