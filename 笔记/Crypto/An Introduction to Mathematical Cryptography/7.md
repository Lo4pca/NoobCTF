# Lattices and Cryptography

又到了学线性代数的季节，以及一个永远搞不明白索引的我

## Exercise 7.10

假设A是一个n阶方阵，里面的每一项记为 $a_{ij}$ 。对每组索引i和j，让 $A_{ij}$ 表示删去A的第i行，第j列后组成的n-1阶方阵。再定义一个新矩阵B，其ij元 $b_{ij}$ 满足公式 $b_{ij}=(-1)^{i+j}det(A_{ji})$ （注意 $b_{ij}$ 是子矩阵 $A_{ji}$ 的行列式，索引反过来了）。称矩阵B为A的伴随矩阵（adjoint）

1. 证明 $AB=BA=det(A)I_n$

两个方阵A和B的积C的ij元 $c_{ij}=\sum_{k=1}^na_{ik}b_{kj}$ 。带入题目给出的前提得到 $c_{ij}=\sum_{k=1}^na_{ik}(-1)^{k+j}det(A_{jk})$

假如i=j，则这个式子等同于按第 i 行展开的拉普拉斯展开，因此 $c_{ii}=det(A)$

假如i不等于j，将A矩阵的第j行换成第i行（其余行不变），构成的新矩阵称为M。这意味着M有两行相同，因此det(M)=0。将M按第j行展开，得到 $det(M)=\sum_{k=1}^{n}M_{jk}(-1)^{j+k}det(M_{jk})$ 。注意到由于M和A只有第j行不同，所以 $det(M_{jk})=det(A_{jk})$ ；且由于M的第j行与A的第i行相同，也有 $a_{ik}=M_{jk}$ 。由此得到i不等于j时， $c_{ij}=0$ ， $AB=det(A)I_n$ 。BA的证明过程同理

3. 假设A拥有整数项。证明 $A^{-1}$ 存在且也有整数项当且仅当 $det(A)=\pm 1$

有个定理：假如A和B都是n阶方阵，则det(AB)=det(A)det(B)

$det(A)=\pm 1$ 时，用2的结论 $A^{-1}=\frac{1}{det(A)}B$ 就能看出 $A^{-1}=\pm B$ ，而B也是整数矩阵

如果整数矩阵 $A^{-1}$ 存在，则 $AA^{-1}=I_n,det(A^{-1})=\frac{1}{det(A)}$ 。 $det(A^{-1})$ 一定是整数，所以唯一可能的情况是 $det(A)=\pm 1$

## Exercise 7.9

证明 $R^m$ 的子集是格（lattice）当且仅当它是离散加法子群（discrete additive subgroup）

（ $R^m$ 是m维欧几里得空间，是由所有拥有m个实数的有序元组构成的集合）

https://perso.univ-rennes1.fr/christophe.mourougane/enseignements/2010-11/THNO/reseau.pdf 的第二页有一道类似题目。注意这份资料对离散的定义与本书不同，不过在这题的背景下两者定义是等价的

格到离散加法子群这个方向似乎不难，这里不再抄一遍了（

反过来就开始让人头晕了。说G是 $R^n$ 中的离散加法子群，我们使用归纳法证明它是一个格。归纳假设为：
- 对于所有张成空间维数小于m的 $R^n$ 中的离散加法子群，结论都成立——它们都是格

假设{ $g_1,...,g_m$ }是G的一组极大线性无关组（maximal linearly independent subset），V是 { $g_1,...,g_{m-1}$ }张成的子空间，再假设 $G_0=G\cap V$ 。 $G_0$ 是m-1维的，且 $G_0$ 是两个子群的交集（所以 $G_0$ 是子群），也是离散子群G的子集（所以 $G_0$ 也是离散的），因此根据归纳假设，它是一个格

既然 $G_0$ 是一个格，那么根据定义，存在线性无关元素 $h_1,...,h_{m'}$ 可以生成 $G_0$ 。又因为 $g_1,...,g_{m-1}\in G_0$ ，可得知m'=m-1。接下来为了方便，我们用 $h_1,...,h_{m-1}$ 替换掉 $g_1,...,g_{m-1}$ （下文提到的 $g_i$ 指的都是 $h_i$ ）

然后构造一个集合T，T中的元素为G中所有形如 $a_1g_1+...+a_mg_m$ 的元素x。其中 $a_i\in R,0\leq a_i$ < 1, $0\leq a_m\leq 1,i=1,...,m-1$

很明显T是有界的，因此也是有限的，因为G本身是离散的。于是我们取T中 $a_m$ 分量最小但不为0的x',称 $x'=b_1g_1+...+b_mg_m$ 。自然有{ $g_1,...,g_{m-1},x'$ }线性无关（因为只有x'携带了 $g_m$ 方向的分量）

接下来开始证明 { $g_1,...,g_{m-1},x'$ } 可以生成G。假设对于所有G中的元素g，我们选择整数系数 $c_i$ ，使得 $g'=g-c_mx'-c_1g_1-...-c_{m-1}g_{m-1}$ 在T中，且 g'的 $g_m$ 分量的系数小于 $b_m$ ，并且不是负数。因为构造x'时已经保证 $b_m$ 是T中最小的，所以g'的 $g_m$ 分量的系数只能是0，说明 $g'\in G_0$

得知 $g'\in G_0$ 即g'可以写成 $g_1,...,g_{m-1}$ 的整数线性组合后， $g'=g-c_mx'-\sum_{i=1}^{m-1}c_ig_i\Rightarrow g=g'+c_mx'+\sum_{i=1}^{m-1}c_ig_i\Rightarrow g=(\sum_{i=1}^{m-1}d_ig_i)+c_mx'+\sum_{i=1}^{m-1}c_ig_i=c_mx'+\sum_{i=1}^{m-1}(c_i+d_i)g_i$ 。 $c_m,c_i,d_i$ 都是整数，因此任何 $g\in G$ 都可以表示为 { $g_1,...,g_{m-1},x'$ } 的整数线性组合，G是格

（到底是怎么想到这样证明的？）

## Exercise 7.14

假设 $L\subset R^m$ 是n维的格， $v_1,...,v_n$ 是L的基。注意我们允许n小于m。 $v_1,...,v_n$ 的Gram矩阵为 $Gram(v_1,...,v_n)=(v_i·v_j)_{1\leq i,j\leq n}$

1. 假设 $F(v_1,...,v_n)$ 是`Proposition 7.20`中描述的矩阵`7.11`，只是现在我们允许 $F(v_1,...,v_n)$ 为n乘m矩阵，即它无需是方阵。证明 $Gram(v_1,...,v_n)=F(v_1,...,v_n)F(v_1,...,v_n)^t$ ，其中 $F(v_1,...,v_n)^t$ 是转置矩阵

矩阵`7.11`的结构是把每个向量 $v_i$ 作为矩阵的行，那么转置后就是把 $v_i$ 作为矩阵的列。根据矩阵乘法的定义“行乘列”，这就等同于 $(v_i·v_j)_{1\leq i,j\leq n}$

2. 证明 $det(Gram(v_1,...,v_n))=det(L)^2$ ，其中det(L)是L的任意一组基张成的平行多面体的体积（n=m的情况可能会更好证）

https://math.stackexchange.com/questions/633868/gram-determinant-equals-volume

n=m的情况是一个可以直接用的结论：由一组向量张成的平行多面体的有向体积，等于以这些向量在标准正交基下的坐标为列构成的矩阵的行列式

唯一的问题是， $L\subset R^m$ 是一个由n个m维向量张成的格，没有办法直接套上述结论：过程中无法把 $det(Gram(v_1,...,v_n))$ 拆成 $det(F(v_1,...,v_n))^2$ 。但是吧，虽然你有m维向量，但终究还是个n维格。根据直觉，对于一个由n个m维向量张成的n维的格，一定存在一个由n个n维向量张成的n维的格与之等价，那么后者就能直接套用公式了

（不过题目的意思是不是要我们额外证明n=m的情况？）

## Exercise 7.15

假设L是一个格，F是L的基本域（fundamental domain）。此练习尝试证明 $lim_{R\rightarrow\infty}\frac{\sharp(B_R(0)\cap L)}{Vol(B_R(0))}=\frac{1}{Vol(F)}$

1. 考虑完全包裹在 $B_R(0)$ 中或与 $B_R(0)$ 有非平凡交集的F的平移变换，证明：

$$\cup_{v\in L,F+v\subset B_R(0)}(F+v)\subset B_R(0)\subset\cup_{v\in L,(F+v)\cap B_R(0)\not=\empty}(F+v)$$

感觉这个式子虽然看起来长，但都是明显成立的。`Proposition 7.18`已经说明了F+v可以覆盖 $R^n$ 中的每一个向量，那么自然有“完全包裹在球 $B_R(0)$ 中的部分是 $B_R(0)$ 的子集，完全盖住球的部分是 $B_R(0)$ 的父集”

2. 取`1`中集合的体积，证明 #{ $v\in L:F+v\subset B_R(0)$ }·Vol(F) $\leq Vol(B_R(0))\leq$ #{ $v\in L,(F+v)\cap B_R(0)\not =\empty$ }·Vol(F)

提示：`Proposition 7.18`说明了F的不同平移之间没有交集

`Figure 7.2`太好用了，导致要证的命题似乎又过于明显了。球里完全包含的F的数量乘上F的体积一定小于等于球自身的体积；球的体积一定小于等于包裹住它的F共同的体积。由于F+v互不相交，不会有重复计算的部分

3. 证明与 $B_R(0)$ 相交但不被 $B_R(0)$ 完全包裹的F的平移变换的数量相比于被 $B_R(0)$ 完全包裹的平移变换的数量来说很小

有人问过这题： https://math.stackexchange.com/questions/3185766/the-number-of-lattice-points-inside-r-radius-ball-centered-at-origin

考虑满足 $F\subset B_c(0)$ 的最小半径c，与 $B_R(0)$ 相交但不被完全包裹的部分分布在 $B_{R+c}(0)$ 和 $B_{R-c}(0)$ 之间的夹层。利用球体积公式 $Vol(B_R(0))=\omega_n R^n$ ，夹层的体积为 $\omega^n(R+c)^n-\omega^n(R-c)^n$

完全被包裹的部分大致分布于 $B_{R-c}(0)$ 内，用神奇的wolfram alpha计算极限可得到 $lim_{R\rightarrow\infty}\frac{\omega^n(R+c)^n-\omega^n(R-c)^n}{\omega^n(R-c)^n}=0$ （没想到直接就是0了，甚至不用除去L的体积来估算格点数量）

4. 用`2`和`3`证明 $Vol(B_R(0))=\sharp(B_R(0)\cap L)·Vol(F)+$ (更小的项)。整体除以 $Vol(B_R(0))$ 并让 $R\rightarrow\infty$ 来证明最开始提到的式子

$\sharp(B_R(0)\cap L)$ 可拆成“完全在球里的部分”和“与球有相交的部分”，假设是X+a。那么 $Vol(B_R(0))=(X+a)·Vol(F)=X·Vol(F)+a·Vol(F)$ 。结合`2`的结论，有 $X·Vol(F)\leq Vol(B_R(0))\leq X·Vol(F)+a·Vol(F)$ 。已知 $\frac{a·Vol(F)}{X·Vol(F)}\rightarrow 0$ ，所以也有 $\frac{a·Vol(F)}{Vol(B_R(0))}\rightarrow 0$

## Exercise 7.27

本练习介绍如何在 $R_m=\frac{(Z/mZ)[x]}{x^N-1}$ 中寻找逆元，其中 $m=p^e$ ，p为质数

1. 设 $f(x)\in Z[x]/(X^N-1)$ 是一个多项式。假设我们已经找到了一个满足 $f(x)\times F(x)\equiv 1\mod p^i,i\geq 1$ 的多项式F(x)。证明 $G(x)=F(x)\times(2-f(x)\times F(x))$ 满足 $f(x)\times G(x)\equiv 1\mod p^{2i}$

$fG-1=2fF-f^2F^2-1=-(fF-1)^2$ 。从假设可以得知 $fF=kp^i+1$ ，所以 $(fF-1)^2=k^2p^{2i}\equiv 0\mod p^{2i}$

## Exercise 7.32

选择NTRUEncrypt公共参数(N,p,q,d)时要求gcd(p,q)=1。证明如果p|q，那么Eve即使在不知道私钥的情况下也可以轻易获取m（提示：先做p=q的情况）

NTRUEncrypt过程见432页。明显在p=q的情况下，密文e等于明文m

然后我又开始犯病了。我想的是计算d=gcd(p,q)，接着拿d乘上e。这样prh部分会被模掉，留下md，最后找d的逆元恢复m。但是有没有一种可能，直接拿e模p就好？

## Exercise 7.37

选择NTRUEncrypt公共参数(N,p,q,d)时要求N是质数。关于为什么，假设N是偶数；解释Eve如何通过求解N维（而不是2N维）的格问题恢复私钥。提示：利用自然映射（natural map） $Z[x]/(x^N-1)\rightarrow Z[x]/(x^{N/2}-1)$

这是个论文题： https://link.springer.com/chapter/10.1007/3-540-44987-6_12

提示的内容是第5页的`Theorem 1`。像提示那样的自然映射不局限于2，任何非平凡因子都可以构造那样的映射，而且这是个环同态

教材438页给出了`The NTRU Lattice`，是通用的格构造；将这个格用环同态降维后得到的就是论文第6页`5.1`节的内容，求解格问题可以获取私钥f的部分信息。最后跟着第7页的`5.3`可恢复完整的f

## Exercise 7.24

$a(x)\in(Z/qZ)[x]$ ，q是质数

1. 证明 $a(1)\equiv 0\mod q$ 当且仅当(Z/qZ)[x]中有(x-1)|a(x)

$a(1)\equiv 0\mod q$ 说明多项式a的全部系数之和是q的倍数。那么在Z/qZ的a(x)中，这相当于我们减去了一次系数之和，得到的结果就是a(x-1)减去原多项式的常数项，很明显是x-1的倍数

然后我不知道怎么证另一个方向了。不靠直觉的正确做法是对a(x)进行带余除法：a(x)=(x-1)b(x)+r(x)。r的次数小于x-1的次数，那么只能是0。这意味着要么r(x)整体是0，要么r(x)是一个常数项，所以a(x)=(x-1)b(x)+c。代入x=1可以算出c=a(1)。这样就同时证明了两个方向

2. 假设 $a(1)\equiv 0\mod q$ 。证明a(x)在 $R_q$ 中不可逆

构造一个由 $R_q$ 到Z/qZ的映射： $\phi(f(x)):f(1)$ 。 $R_q$ 中把f(x)和 $f(x)+(x^N-1)g(x)$ 视作同一个元素，而代入x=1时 $(x^N-1)g(x)=0$ ；因此映射的值唯一且与代表元的选择无关，映射是良定义的

$\phi$ 的同态性质显然。假设a(x)可逆，那么便有 $\phi(ab)=\phi(1)\Rightarrow a(1)b(1)=1$ 。但是条件中有 $a(1)\equiv 0\mod q$ ，故矛盾。因此a(x)不可逆

## Exercise 7.34

Alice用p=3的NTRUEncrypt给Bob发送消息
1. 假设Alice用相同的r(x)加密了两条不同的明文 $m_1(x)$ 和 $m_2(x)$ 。解释Eve如何用 $e_1$ 和 $e_2$ 推出 $m_1(x)$ 约九分之二的系数

$e_1-e_2=m_1(x)-m_2(x)$ 。由于p=3，m可能的系数为0，1，2=-1；于是两者之差可能的系数为-2，-1，0，1，2（这里能同时出现2和-1是因为相减的操作在模q下发生）。每个差都能排除m不可能的系数。其中当差的系数为2或-2时，可以确定 $m_1$ 对应的系数一定是1或-1；对应题目九分之二的概率

3. 假设Alice用不同的随机元素 $r_1(x),r_2(x)$ 加密同一个明文m(x)，尝试构造与`1`类似的攻击（提示：先假设h(x)在 $R_q$ 下有逆元。此题没有这个假设会更难）

假如h(x)有逆元的话，两个密文相减并乘上h(x)和p在q上的逆元即可得到`1`类似的结构，攻击方式也类似

不可逆的情况得看solution manual了。通常来说h(x)都是不可逆的，因为g的构造导致g(1)=0，用`Exercise 7.24`的结论得出其不可逆。这种情况下需要用CRT分解 $R_q\cong Z_q[x]/(x-1)\times Z_q[x]/\phi(x)$ ，其中 $\phi(x)=x^{N-1}+x^{N-2}+...+x-1$ 且 $x^N-1=(x-1)\phi(x)$ 。h(x)映射到这个乘积环上的像是 $(0,h(x)\mod\phi(x))$ ，于是求第二个分量的逆元是可行的

## Exercise 7.36

本题介绍如何将NTRU明文恢复问题转成CVP

1. 证明向量(pr,e-m)在 $L_h^{NTRU}$ （定义在`7.11.1`开头）中

和`Proposition 7.59`的证明思路差不多

2. 证明`1`的向量几乎一定是与已知向量(0,e)最近的格向量，因此求解CVP可得到m（为了方便，可以假设 $p=3,d\approx\frac{N}{3},q\approx 2N$ ，与`Proposition 7.61`中的假设一致）

416页提到了“Gaussian heuristic for CVP”：一个随机点w和与它最近的格向量v满足 $||v-w||\approx σ(L)$ （定义在415页的`7.21`）

(0,e)-(pr,e-m)=(-pr,m)。算欧几里得范数 $||\delta||^2=\sum^{N-1}_{i=0}(pr_i)^2+\sum^{N-1}_{i=0}m_i^2$ 。p=3，因此 $m_i$ 的系数和 $r_i$ 一样，都在{-1,0,1}中,或者说有 $\frac{2}{3}$ 的概率不为0。那么x的长度大约为 $\sqrt{\frac{2N}{3}(p^2+1)}$ 。这个值相比σ(L)更小，因此CVP有高概率解决这个问题

## Exercise 7.46

假设V是向量空间， $W\subset V$ 是V的向量子空间， $W^{⊥}$ 是V中W的正交补（orthogonal complement）

2. 证明每个向量 $v\in V$ 可以唯一地写成v=w+w'， $w\in W,w'\in W^{⊥}$ （称V是子空间W和 $W^{⊥}$ 的直和）

直觉上很明显：根据正交补的定义，W和 $W^{⊥}$ 中的向量在方向上完全没有重合的部分，且两者覆盖了向量空间V中所有的方向。但是我们需要稍微严谨一点的证明

取W的一组标准正交基{ $e_1,...,e_k$ }，找v在W上的正交投影： $w=\sum_{i=1}^k<v,e_i>e_i$ 。令w'=v-w，查看w'与每个 $e_i$ 的关系： $<w',e_i>=<v-w,e_i>=<v,e_i>-<w,e_i>=<v,e_i>-<v,e_i>=0$ （ $<w,e_i>=<v,e_i>$ 可以利用内积的双线性推出），所以 $w'\in W^{⊥}$

至于唯一性：如果 $v=w_1+w_1'=w_2+w_2'$ ，则 $w_1-w_2=w_2'-w_1'$ 。左边属于W，右边属于 $W^{⊥}$ ；而正交补的定义保证了W与 $W^{⊥}$ 的交集只有{0}，因此 $w_1=w_2,w_1'=w_2'$

## Exercise 7.54

假设 $L\subset Z^n$ ，我们在`Theorem 7.71`中证明了LLL算法会终止且有着多项式运行时间。但这个假设不是必要的。对于任意格 $L\subset R^n$ ，可以证明出相同的结论。你可以假设你的计算机能够在R中做精确计算，虽然实际情况下需要考虑精度问题（提示：用Hermite’s theorem推导出与L中最短向量长度有关的下限，对应`Theorem 7.71`的证明中出现的D）

查看458页开始的`Theorem 7.71`的证明，发现前半部分的证明与格点是整数还是实数无关；直到459页的`7.61`。在整数格的假设下，易得出D大于等于1；而在实数的假设下我们需要重新计算D的下限，用Hermite’s Theorem： $||v||^2\leq\gamma_n det(L)^{\frac{2}{n}}\Rightarrow(\frac{||v||^2}{\gamma_n})^n\leq det(L)^{2}$ 。配合`Exercise 7.14`第2和第4小题的结论，得出 $||v_1^{\*}||^2...||v_n^{\*}||^2\geq(\frac{||v||^2}{\gamma_n})^n$ ,前者正好是 $d_l$ 的定义，说明 $d_l$ 是正数；而作为多个 $d_l$ 的积，D也一定是正数，故只能乘上有限次数的 $\frac{3}{4}$

运行时间为多项式的证明也与原证明相同

(跳过了`Exercise 7.52`，因为书中提示这题很难，solution manual写了好几页)

## Exercise 7.8

假设 $L\subset R^m$ 是加法子群。对一个正整数 $\epsilon$ ,L满足 $L\cap$ { $w\in R^m:||w||$ < $\epsilon$ }={0}。证明L是离散的，从而是格

题目给的条件等同于说“至少存在一个正整数 $\epsilon$ ，使得L中距离原点最近的点大于 $\epsilon$ ”。这样就足够看出条件`7.8`（判断L是离散加法子群的条件）了，因为加法子群对加法和减法封闭，意味着L中所有的点至少相距一个 $\epsilon$

找deepseek要了严谨一点的语言：

任取 $x,y \in L, x \ne y$ ，则 $x-y \in L\setminus$ {0}，所以 $||x-y|| \ge \epsilon$ ,因此 $L$ 中任意不同两点距离 $\ge \epsilon$

取 $\delta = \epsilon/2$ > 0，对任意 $v \in L$ ，有 $B_\delta(v) \cap L =$ {v}，故L中每个点在 $\mathbb{R}^m$ 中孤立，从而L是离散加法子群

## Exercise 7.28

假设在 $R^N$ 中固定一个向量a

1. 假设b是一个N维的向量，我们在{-1,0,1}中随机选择其系数。证明 $||b||^2$ 和 $||a\*b||^2$ 的期望值为 $E(||b||^2)=\frac{2}{3}N,E(||a\*b||^2)=||a||^2E(||b||^2)$

第一个很明显，第二个考虑\*的定义为`Proposition 7.39`，则 $||a*b||^2=\sum_{k=0}^{N-1}c_k^2=\sum_{k=0}^{N-1}(\sum_{i=0}^{N-1}a_ib_{k-i})^2=\sum_{k=0}^{N-1}\sum_{i=0}^{N-1}\sum_{j=0}^{N-1}a_ia_jb_{k-i}b_{k-j}$ 。换一下顺序，得到 $\sum_{i=0}^{N-1}\sum_{j=0}^{N-1}a_ia_j\sum_{k=0}^{N-1}b_{k-i}b_{k-j}$ 。设u=k-i， $d\equiv i-j\mod N$ ，记后面这部分的求和为 $S_{ij}=\sum_{k=0}^{N-1}b_ub_{u+d}$ ;取期望 $E(||a\*b||^2)=\sum_{i,j}^{N-1}a_ia_jE(S_{ij})$ （注意题目说a是固定向量，所以期望值就是它自身）。分析 $E(S_{ij})$ 的值：如果d=0，则 $E(b_ub_{u+d})=E(b_u^2)$ ;如果d不为0，则由于 $E(b_u)=0,E(S_{ij})=0$ 。这样便允许我们将两层 $\sum$ 合并成一层，得到 $\sum_{i=0}^{N-1}a_i^2E(b_i^2)=E(b_i^2)\sum_{i=0}^{N-1}a_i^2=||a||^2E(||b||^2)$

2. 更一般的，假设在{-T,-T+1,...,T-1,T}中随机选择b的系数，类似`1`这样计算 $E(||b||^2)$ 与 $E(||a\*b||^2)$

$E(||b||^2)=N\frac{2}{2T+1}\sum_{i=1}^Ti^2=\frac{NT(T + 1)}{3}$

$E(||a\*b||^2)$ 的证明和上题一样，deepseek大手发力了（

3. 现在假设b的每个系数是从-R到R范围中独立随机选择的实数。证明 $E(||b^2||)=\frac{R^2N}{3},E(||a\*b||^2)=||a||^2E(||b||^2)$

提示：最直接的做法是用continuous probability theory。另一种方法是让b从集合{jR/T: $-T\leq j\leq T$ }中独立随机选择系数，重做`2`，然后让 $T\rightarrow\infty$

-R到R之间任意一个实数的概率需要用概率密度函数f(x)计算。这个函数不代表概率，而是“概率的浓度”。对于连续随机变量，我们没有办法再计算P(X=x)的概率，因为它是0；只能计算某个区间的概率， $P(a\leq X\leq b)=\int_a^bf(x)dx$ ，即概率等于密度函数曲线下的面积。另外，密度函数必须满足 $\int_{-\infty}^{\infty}f(x)dx=1$

这题的区间长度为2R，且因为是均匀分布，密度函数是一个常数函数C。利用面积“底乘高”的定义，2R\*C=1，得出密度函数为 $\frac{1}{2R}$ 。接着就能算连续期望了： $E(b_i^2)=\int_{-R}^R\frac{x^2}{2R}dx=\frac{R^2}{3}$ (其实和离散的情况 $\sum x\*P(X=x)$ 类似，只是 $\sum$ 换成了积分，“密度函数乘dx”替换P(X=x))。最后 $E(||b^2||)=\sum_{i=1}^NE(b_i^2)=\frac{NR^2}{3}$

$E(||a\*b||^2)$ 的证明仍然和`1`一样，因为那个证明只用到了 $E(b_i)=0$ 和独立分布两个条件

## Exercise 7.33

假设Alice选择NTRUEncrypt公共参数时让q=N

1. 在环 $Z[x]/(x^N-1)$ 中替换变量x=y+1。说明NTRU格在这种情况下结构更简单

替换变量后得到的环为 $Z[y]/((y+1)^N-1)$ 。用二项式定理展开 $(y+1)^N$ 后可以发现，在模数q等于N且为质数的情况下， $\binom{N}{k}\equiv 0\mod q$ ；这意味着 $y^N=0$ 。此时这个环中的乘法不再是循环卷积，而是高次项截断乘法：高次项直接消失，而不是绕回低次项。那么构造NTRU格时右上角不再需要h的循环矩阵，取而代之的是h的系数组成的下三角矩阵

## Exercise 7.35

这个练习描述了NTRUEncrypt的一个变种，可以省去解密的一个步骤，代价是参数大小更大。假设NTRUEncrypt私钥多项式f(x)和g(x)满足 $f(x)=1+pf_0(x)\equiv 1\mod p,g(x)=pg_0(x)\equiv 0\mod p$ ,加密变成了 $e(x)\equiv h(x)\*r(x)+m(x)\mod q$ （h(x)前不需要乘p了）

1. 证明如果q足够大，下述算法可以正确解密消息：
- 计算 $a(x)\equiv f(x)\*e(x)\mod q$ 并中心提升（center-lift）至R中的一个元素
- 计算 $a(x)\mod p$ ，结果为m(x)

注意这样便不再需要给a(x)乘上 $f(x)^{-1}\mod p$

假设h(x)还是 $F_q(x)\*g(x)\mod q$ （430页的`7.32`），m(x)仍然是某个 $R_p$ 中多项式的中心提升（431页）

a(x)=f(x)\*e(x)=f(x)\*(h(x)\*r(x)+m(x))=g(x)\*r(x)+f(x)\*m(x) (均在模q下进行)。只要q足够大， $a(x)\mod q$ 中心提升后的结果就是a(x)在R中的精确值

a(x)模p后，由于g(x)模p等于0，f(x)模p等于1，因此结果就是m(x)

2. 假设我们选择 $f_0,g_0\in\tau(d,d)$ ，并假设m是三元的（ternary）。证明在q > 8dp + 2的情况下可以正确解密出m（提示：模仿`Proposition 7.48`的证明）

计算g(x)\*r(x)+f(x)\*m(x)在R中精确值的系数最大值：
- 模仿432页的证明，假设r(x)的d个正数系数和d个负数系数完全对上了g(x)的正数系数和负数系数，则最大值为 $pg_0r=p(2d)=2dp$
- f(x)\*m(x)同理： $(1+pf_0)m=m+pf_0m=1+2dp$
- 2dp+1+2dp=4dp+1，4dp+1 < $\frac{q}{2}\Rightarrow q$ > 8dp+2

## Exercise 7.38

假设Alice和Bob用NTRUEncrypt交换信息。Eve拦截了一条密文e(x)且她已经知道m(x)的一部分。更准确地说，假设Eve已经知道了m(x)的t个系数。解释如何构造并求解一个2N-2t维的格上的CVP来找到m(x)

如果目标是m(x)的话，唯一可用的关系是e ≡ pr\*h+m。题目要求用cvp来做，所以m需要出现在格里，然后构造另外一个不在格里的向量，使其与m的距离最近。但我对如何构造出2N-2t维的格毫无头绪。继续让deepseek上，对不对完全取决于它有没有出幻觉

用循环矩阵H表示与h(x)的卷积。假设已知的m的t个系数组成 $m_1$ ，剩下未知的系数长度为N-t，记为 $m_2$ 。按相同方式分割 $e=[e_1,e_2],k=[k_1,k_2],H=[H_1,H_2]$ (前t行与后N-t行)，可得两个式子：
- $pH_1r=e_1-m_1$
- $pH_2r+m_2=e_2$

第一个式子中的未知项只有r，但 $H_1$ 的维度是t\*N，方程不是齐次的。因此只能尝试得出一个特解 $r_0$ ，然后找到一个N\*(N-t)维的矩阵R（N-t是因为未知变量的维度是N-t），满足 $H_1R\equiv 0\mod q$ 。这样r就能被参数化为 $r\equiv r_0+Ru\mod q,u\in Z^{N-t}$ 。这一步可以削减未知向量的维度，问题变成求u

将上述结论替换到第二个方程中，得 $(pH_2R)u+m_2\equiv e_2-pH_2r_0\mod q$ 。记 $A=pH_2R,b=e_2-pH_2r_0$ ，方程简化为 $Au+m_2\equiv b\mod q$ 。一般到这个时候，我的做法是把b挪到左边去。但此时A是N-t维的方阵，u和 $m_2$ 也是N-t维，拼在一起已经是2N-2t了；再挪个b就变成2N-2t+1维的格了

我们换个思路，找一个满足方程的特解，称为t，那么目标短向量v与t的差为d=v-t。把d代入进原方程： $A(v_u-t_u)+(v_m-t_m)\equiv Av_u+v_m-At_u-t_m\equiv 0\mod q$ 。这下就过原点了，且正好是2N-2t维。现在我们已知t且构造好了格，拿这个格与t做cvp，得到的就是t与v的差值，最后相加就能得到真实的v了

deepseek确实能替代我，我之前从来没有这样理解过cvp。我之前的思路类似直接拿 $Au+m_2$ 生成格，然后通过b的可能取值取个中位数，直接拿中位数和格做cvp

## Exercise 7.43

假设 $b_1,b_2$ 是向量， $t=b_1·b_2/||b_1||^2,b_2^{*}=b_2-tb_1$ 。证明 $b_2^{*}·b_1=0$ ,且 $b_2^{*}$ 是 $b_2$ 在 $b_1$ 的正交补上的投影

$b_2^{*}·b_1=b_1\cdot b_2-tb_1^2=b_1\cdot b_2-b_1\cdot b_2/(||b_1||^2)\cdot||b_1||^2=0$

由于 $b_2^{*}·b_1=0$ ，两者正交，故 $b_2^{*}$ 肯定在 $b_1$ 的正交补上。考虑到正交补的定义且 $b_2^{*}=b_2-tb_1$ ，猜测t的作用是减去 $b_2$ 中所有 $b_1$ 上的分量。又是只会猜，不会写数学语言的一天

设 $b_1$ 的正交补为U， $b_2^{*}$ 是 $b_2$ 在U上的投影等价于 $b_2^{*}\in U$ 且 $b_2-b_2^{*}$ 垂直于U。第一个条件上面已经证过了，第二个条件等价于 $b_2-b_2^{*}=kb_1$ ，因为整个垂直于U的空间都是 $b_1$ 生成的。把它变形一下代到 $b_2^{*}·b_1=0$ 中，解得 $k=b_1\cdot b_2/||b_1||^2$ ，正好就是t的定义

## Exercise 7.44

a和b是 $R^n$ 中的非零向量

1. 什么样的值 $t\in R$ 可最小化||a-tb||？（提示：最小化 $||a-tb||^2$ 的值更简单）

看到“最小化”的第一刻我竟然没有反应过来这题是导数。||a-tb||带平方根，求导比较麻烦；平方后消除了问题，且不改变对距离的比较。 $f(t)=||a-tb||^2=||a||^2-2t(a\cdot b)+t^2||b||^2,f'(t)=-2(a\cdot b)+2t||b||^2$ 。令f'(t)=0，得 $t=\frac{a\cdot b}{||b||^2}$ （突然变成`Exercise 7.43`）

## Exercise 7.59

1. 假设 k 是一个 10 位整数，并且计算 $\sqrt{k}$ 时，小数点后的前 15 位数字是 418400286617716。找到k（提示：将其看成一个格问题）

这和`Exercise 1.49`差不多，我当时没做出来，现在还是没做出来

solution manual给的公式和之前一样，不过这次额外给了格的构造。看来我第一章的猜测是错的，因为manual直接使用 $\beta$ 构造，没有额外乘回 $10^d$ 将其化为整数，而是乘了c和t,应该都是权重。于是我构造出了这个玩意：
```py
c=2^256
beta=0.418400286617716
beta_square=beta^2
t=2^256
M=matrix(QQ,[
    [1,0,0,c],
    [0,1,0,c*beta],
    [0,0,t,c*beta_square]
])
print(M.LLL())
```
自然是不对的。我也不知道哪里出了问题。按照manual的说法，B和A会出现在格里，类似(B,A,1)；但A和B真的算小吗？